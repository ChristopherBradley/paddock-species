{
  "stage": "2024_9KM_NATIONAL_MAP_COMPLETE",
  "updated": "2026-09-11",
  "description": "National crop-type map, one year at a time, via src/paddocks/run_national.sh. See PAPER_PLAN.md E8 and output/PROJ_NOTES.md 2026-09-04 entry for full narrative context.",
  "years_done": [
    "2023",
    "2024"
  ],
  "years_remaining_in_order": [
    "2022",
    "2017",
    "2018",
    "2019",
    "2020",
    "2021",
    "2025"
  ],
  "current_year": "2022",
  "current_year_state_2026-09-04": {
    "aois_total": 99465,
    "composites_pct_before_repair": 99.2,
    "segmented_pct_before_resam": 69.4,
    "predicted_chunks_before": 0,
    "first_loop_check_2026-09-04": "segmented 70.9% (up from 69.4%), 54 of our jobs in queue (12 running), repair-check clean (0 ran yet, 4 still queued/running, none vanished). Overall PBS queue at 177/180 MAXQ headroom (mostly unrelated shelterbelts jobs) -- almost no room to submit the remaining 66 resam jobs this check; next check should retry `resam` as headroom frees.",
    "second_loop_check_2026-09-04": "segmented 73.6% (up from 70.9%), overall queue 169/180 (11 headroom). repair-check still clean (4 repair presegment jobs still queued/running, none vanished, 0 finished yet). Re-ran `resam`: 25,344 tiles still unsegmented -> 102 jobs needed, submitted 11 with available headroom, 91 remain queued behind MAXQ for the next check.",
    "third_loop_check_2026-09-04": "segmented 82.2% (up from 73.6%) -- big jump as queue drained (unrelated shelterbelts jobs finished). Overall queue dropped to 18/180 (164 headroom). repair-check confirmed the 4 repair presegment jobs (r000-r003, job IDs 178189142/143/144/147) are actively Running, not stalled -- just not finished yet within their 6h walltime. Re-ran `resam` with the freed headroom: ALL remaining 68 jobs submitted in one pass (16,953 tiles) -- resam is now fully caught up, nothing left queued behind MAXQ for this stage. Next check should see segmented climbing toward 100% as these + the repair chain finish.",
    "fourth_loop_check_2026-09-04": "segmented 85.5% (up from 82.2%). Queue at 66/180 (63 Q, 3 R -- resam jobs mostly waiting on gpuvolta slots, normal scheduling, not an issue). repair-check: 1 of 4 repair presegment jobs finished (3 still running, none vanished) -- composites count unchanged at 98711 so far, repair-sam not started yet (jobmap.txt doesn't exist -- waits for repair presegment to progress further). Re-ran `resam`: 13,671 tiles still unsegmented, submitted all 55 needed jobs (ample headroom, 114 free). resam has no backlog again.",
    "fifth_loop_check_2026-09-04": "segmented 93.3% (up from 85.5%). Queue 96/180 (79 Q, 18 R). repair-check still clean: 1 of 4 repair presegment jobs done, 3 running at 3:43-3:45 elapsed of 6h walltime (not stalled, just slow -- DEA datacube pooler contention likely). Composites still 98711, repair-sam still not started (waits for repair presegment). Re-ran `resam` (took ~5 min this time vs usual seconds -- directory-listing contention from the higher job count, not a fault): 5,215 tiles still unsegmented, submitted all 21 needed jobs with 85 headroom. resam has no backlog. Getting close to segmented~100%.",
    "sixth_loop_check_2026-09-04_GENUINE_FAILURE": "segmented 99.2% (98710/99465 -- matches composites exactly, meaning resam has fully caught up and every currently-composited tile is now segmented; queue down to 3 jobs, all the remaining repair presegment jobs). BUT repair-check found a REAL failure, not a scheduling quirk: repair job r003 (PBS ID 178189147) is NOT still running -- it exited with status 1 after `presegment: 0 built, 0 skipped, 4 failed` and 'ABORT: 4/4 AOIs failed (100%), above --max-fail-frac 10%'. Root cause in its own log: repeated psycopg2.OperationalError retries (2-5, backing off 51s->234s) ending in 'FATAL: server login has been failing, cached error: connect timeout (server_login_retry)' against the DEA datacube. Cost 3.50 SU, wasted (0 composites produced). The other 3 repair jobs (r000-r002, IDs 178189142/143/144) were still Running at ~4:44 elapsed of 6h walltime at check time -- outcome unknown, could hit the same thing near their own walltime end. No other job in the last 6h of logs shows the same error signature -- looks isolated to this batch, not a broad/ongoing gadi-wide DEA outage as far as can be told from here. resam/SAM segmentation is unaffected (pure local file I/O, no datacube dependency) -- only presegment/repair (composite-building) touches the datacube, so this blocks only the last ~753 composite-missing tiles, not the 98,710 already segmented.",
    "diagnosis": "38 of 40 original SAM jobs (8 chunks / ~2488 tiles each, 4h walltime) ran to the wall and were killed mid-chunk -- composites mostly built (99.2%) but segmentation stalled at 69.4%. repair/repair-sam only covers the missing-COMPOSITE subset (754 tiles), not this much larger walltime-killed segmentation gap.",
    "fix": "Added new `resam` subcommand to run_national.sh (after repredict, before merge): batches tiles that HAVE a composite but no segmentation into small ${RESAM_TILES:-250}-tile jobs (vs the original 8-chunk/~2488-tile jobs) so each SAM job actually finishes inside its walltime. samgeo_segment.py is idempotent (skips existing _filt.gpkg), so re-running resam is always safe.",
    "actions_taken": [
      "YEAR=2022 ./run_national.sh repair -- 754 tiles missing composite -> 4 presegment jobs submitted",
      "YEAR=2022 ./run_national.sh resam -- 29,669 tiles missing segmentation -> 119 jobs total, 53 submitted, 66 stopped at MAXQ=180 queue-ceiling headroom (re-run resam to submit the rest as headroom frees)"
    ]
  },
  "next_step_for_2022": "2022 SEGMENTATION COMPLETE (2026-09-07: composites 100.0%, segmented 100.0%, 0 chunks incomplete; the 753-tile gap was 2 zero-byte archive scenes, fixed with dc.load(skip_broken_datasets=True) in samgeo_segment.py -- see PROJ_NOTES 2026-09-07 and memory dea-archive-corrupt-scenes-silently-fail-aoi.md). PRODUCTION MODEL SWITCHED IN CODE 2026-09-08: shipped+sharma6 (macro F1 0.890/0.892) passed an independent review (output/INDEPENDENT_REVIEW_shipped_sharma6.md, ADOPT); production joblib fitted on the full reviewed keep set at derived/models/group3_map_sharma6.joblib (153 features, two-source bundle with `sources`); every MODEL= in run_national.sh and run_map100.sh now points at it; predict_tile.py was extended to run one zonal pass per source (it previously would have silently NaN-filled the second source -- see memory multi-source-model-bundle-silent-nan.md) and verified on one real tile for BOTH the old model (byte-identical to the pre-change baseline) and the new one (n_feat_present median 153). run_national.sh predict also now passes --shape-gate-skip-classes Canola Cereal Legume (abstain_reason still recorded; downstream scripts key off abstain_reason == ''). 2022 has 0 of 320 chunks predicted. USER CONSTRAINT STILL IN FORCE: do NOT launch prediction (or any large) jobs until the user says their other project's compute is done -- as of 2026-09-08 the queue held 3 unrelated `pred_2021` jobs. WHEN CLEARED, in order: (1) `YEAR=2022 ./run_national.sh predict` until 320/320 with 0 incomplete, then `audit`, `merge`, `summary`; (2) re-predict 2023 and 2024 on the new model (delete/rename their pred/ chunks and merged gpkg first, or predict into a fresh dir, so the `-s` guard doesn't skip them), remerge, re-run ABS validation (abs_compare.py) so every Results number reflects the adopted model; (3) the 6 not-yet-started years (2017-2021, 2025) via grid->chunks->presegment->sam+resam->predict->merge->summary; (4) optional: score the 153-feature config once on a genuinely fresh holdout (review caveat b) before submission. DONE 2026-09-08: point 4 -> output/REFERENCE_INTERCOMPARISON.md; point 3a -> output/TILE_BOUNDARY_MERGE.md (tile rasters overlap ~350 m, artefact is duplication; merge_tile_boundaries.py D1-D3/M1/M2 + raster_cut_m; run_national.sh boundary = one small CPU job per year, NOT yet run nationally -- run it after each year's merge; repair_cuts_multiyear.py is an optional second pass needing a donor view). PIPELINE_ARCHITECTURE_AND_TILING.md sec 4 corrected. NOT STARTED: tile-size benchmark (point 3b, needs GPU go-ahead; design in PROJ_NOTES 2026-09-08) and the opt-in zarr time-series write (compute clears 10% with the new model; storage needs the consolidated per-chunk design measured first).",
  "next_step_for_new_years": "For each of 2017, 2018, 2019, 2020, 2021, 2025 in order: `YEAR=<y> GRID_FROM=2024 ./run_national.sh grid` (copies 2024's aois.csv, relabelled -- do NOT re-derive from the NLUM raster or use --offset-seed), then `chunks`, `presegment` (resume/re-run until composites ~100%), `sam` (resume/re-run `resam` until segmented ~100%), `predict` (resume until 320/320 with 0 incomplete), `merge`, `boundary` (cross-tile de-duplication, one small CPU job), `summary`. All stages are idempotent/resumable -- safe to just re-run the same subcommand.",
  "budget": {
    "cumulative_SU_logged_2026-09-04": 24390.7,
    "reference_SU_per_national_year": 6622.6,
    "source_of_reference": "output/NATIONAL_2024_RUN.md section 3 (2024's actual measured cost, includes one repair pass)",
    "projected_remaining_SU_for_2022_partial_plus_6_new_years": "~40000-45000",
    "project_earmark_KSU": 50,
    "user_decision_2026-09-04": "Asked user how to handle exceeding the 50 KSU earmark; user chose to run all remaining years back-to-back without further budget check-ins. Do not re-ask -- just keep logging actual cost via `./run_national.sh cost` in periodic progress notes."
  },
  "monitoring": "Hourly REPORT-ONLY cron bf864fe9 (session-only). The launcher is retired; nothing restarts it. The main session drives boundary -> classified -> comparisons -> GEE by hand and records each job in $M/manual_jobs.txt.",
  "resume_instructions_if_session_drops": "Read this file, then run `cd src/paddocks && YEAR=<current_year> ./run_national.sh status` to get the live picture (this file's percentages are a snapshot, not authoritative -- disk/qstat state always wins). Continue from next_step_for_2022 or next_step_for_new_years as appropriate. Re-arm the hourly loop with `/loop 1h <same task>` if it is not still running.",
  "validation_9km_2026-09-09": {
    "purpose": "score the 9 km final pipeline vs the 3 km production pipeline at the 543 fixed test trials with the temporal-holdout model group3_map_sharma6_le2022.joblib before re-running 2024 at 9 km",
    "state": "DONE 2026-09-09 21:41: output/VALIDATION_9KM.md written. Pooled macro F1 9 km 0.898 vs 3 km 0.885 (349 vs 367 scored of 533); area agreement with trial paddocks 91 % vs 80 % within a factor of 2.",
    "if_it_stalls": "check qstat and /scratch/xe2/cb8590/paddock-species-logs/<jobid>.gadi-pbs.OU; the jobs are resumable (segment_predict.py skips done_stubs.txt); re-run the eval/report lines of waiter_final.sh by hand.",
    "fixes_applied": "segment_predict.py pins 1 BLAS thread per worker (job 178523837 ran zonal 30x slow); eval_map_at_sites.py matches polygons by year; site_tiles.py takes paddock_ha; sampredict.pbs ORCH_EXTRA passthrough; geometry_decision.py --only merges into the JSON",
    "then": "9 km validated at the sites -> re-run 2024 at 9 km per TILE_GEOMETRY_DECISION.md sec 6 (run_national.sh M= override, presegment --output-crs EPSG:3577, sampredict), compare with the retired 3 km map. Check the profile result first if the run will include 2023.",
    "open_question": "2023 9 km tiles run predict's zonal step at 200-500 s/tile vs 16 s for 2024 9 km and 17 s for 3 km 2023; not the node, not threads (pinned re-run still slow). cProfile jobs prof_2023 178553912 / prof_2024 178553913 write benchksu/validation/profile/prof_<year>.out and print pstats in their .OU logs. Resolve before costing 2023-like years; the 6,003 SU/yr in the report is an upper bound from scattered tiles."
  },
  "launch_gate_2024_9km_run_user_decision_2026-09-09": {
    "rule": "Do NOT start the 2024 national run at 9 km until ALL THREE hold: (1) the predict zonal-step slowdown is explained and fixed/controlled (same tile ran 16-482 s across nodes); (2) the projected cost of a full national year at 9 km is confirmed below 2.5 KSU with the fix in place; (3) the PBS queue has headroom that is not taken by the user's neighbouring shelterbelts project's jobs. Once all three hold, the user has approved launching without asking again.",
    "status_2026-09-09": "(1) in progress: not the year, not threads, not the cgroup memory limit; node-dependent and allocation-heavy -- testing numpy MADV_HUGEPAGE / THP compaction stalls (jobs thp_on1-3 / thp_off1-3). (2) not yet: report's 6,003 SU/yr is an upper bound from scattered tiles; FUSED_PREDICT_BENCHMARK's 2,500 SU/yr assumes zonal ~16 s. (3) 55 shelterbelts pred_2022 jobs were running at 18:00 on 2026-09-09.",
    "status_2026-09-09_23:30": "(1) SATISFIED: cause is THP compaction stalls on numpy's MADV_HUGEPAGE hint (6/6 one-tile tests; system time, not user time); NUMPY_MADVISE_HUGEPAGE=0 now exported in every .pbs and in segment_predict.py WORKER_ENV. (2) IN PROGRESS: gate-2 pilot = 48 contiguous parents of the real 9 km 2024 grid (benchksu/pilot9/, NOT the production dir) -> presegment jobs pilot_ps_a/b, then waiter_pilot.sh submits sampredict (11 workers) and writes output/PILOT_9KM_COST.md with the projection vs 2,500 SU. (3) NOT YET: 190 shelterbelts jobs (131 pred_2022 + 59 pi_21_lat*) running at 23:00.",
    "how_to_launch_when_all_three_hold": "TILE_GEOMETRY_DECISION.md sec 6 with M=$D/national2024_9km: grid9_from_2024.py --half-m 4850 (a copy already exists at benchksu/pilot9/aois_9km_2024.csv), chunks (NCHUNK ~60), presegment with PRESEG_EXTRA='--output-crs EPSG:3577', then `sampredict` (SAM+predict co-scheduled, SAM_EXTRA='--prompt-image-only --fp16') instead of sam+predict, then merge, boundary, summary. Record in PROJ_NOTES which evidence satisfied each gate before the first qsub.",
    "status_2026-09-09_23:45": "(1) SATISFIED (THP fix, 6/6 tests). (2) MODELLED, NOT DIRECTLY MEASURED: 48-tile pilot 8.77 SU = 0.183 SU/tile literal (3,392 SU/yr) but 34 % of that job was startup+tail; steady state 10.3 s/tile balanced SAM(GPU 93 % busy)/predict -> 1,000-tile jobs 2,171 SU/yr incl. presegment 425 + 50 (13 % under 2,500, before repair ~5 %). User to say whether the model counts as confirmed, or whether to measure one ~1,000-tile job first (~110 SU; if run in the production dir it is the first 7 % of the year). (3) NOT MET at 23:45: 131 shelterbelts jobs running.",
    "sizing_for_the_run": "export M=$D/national2024_9km; YEAR=2024 NCHUNK=60 ./run_national.sh chunks; PRESEG_EXTRA='--output-crs EPSG:3577' presegment; then PER_SAM=4 SAM_EXTRA='--prompt-image-only --fp16' ./run_national.sh sampredict (~1,060 tiles/job, ~3 h, inside SP_WALLTIME 6 h; PER_SAM=8 would exceed it); then merge, boundary, summary. The 9 km grid file is ready at benchksu/pilot9/aois_9km_2024.csv (half_m 4850); copy it to $M/aois.csv rather than regenerating.",
    "decision_2026-09-10": "User chose option 1: gate 2 accepted on the steady-state model (2,171 SU/yr). User paused shelterbelts submissions; launch when the queue holds FEWER THAN 10 shelterbelts jobs (count by Output_Path containing /shelterbelts/). Pre-approved from then on.",
    "decision_2026-09-10_revised": "OPTION 2: one production slice now (ps_p000 178568915 -> sp_s0 on the same 267 parents), measure SU, project the year. When <10 shelterbelts jobs: projection > 4,000 SU -> HALT and re-evaluate; else launch the full run (pre-approved)."
  },
  "run_2024_9km": {
    "launcher": "$D/national2024_9km/launcher_2024_9km_v2.sh (nohup). Phase A: pilot slice, result in $M/pilot_result.txt. Gate 3 wait. Then full run: presegment remaining chunks (PS_WALLTIME=10h, PS_MEM=8GB, EPSG:3577), sampredict PER_SAM=4 SP_NODEP=1, merge, boundary, summary. Halts on no-progress. v1 launcher (would have launched everything at <10) was killed before it fired.",
    "logs": "$D/national2024_9km/launcher.log (everything) and benchksu/validation/waiter.log (milestones, prefix run2024_9km:, watched by the session's Monitor). Job ids: $M/chunkjobs.txt (presegment), $M/sam/spjobs.txt (SAM+predict).",
    "live_picture": "export M=$D/national2024_9km; YEAR=2024 ./run_national.sh status  (predicted count there is per chunk file and misleading for sampredict; use `cat $M/pred/sp*/done_stubs.txt | sort -u | wc -l`).",
    "stop_restart": "pkill -f launcher_2024_9km; edit; relaunch with nohup setsid bash launcher_2024_9km.sh. NEVER edit the script while it runs (bash reads it incrementally). All stages are idempotent, so a restart just resumes.",
    "expected": "~2,170 SU (PILOT_9KM_COST.md): presegment 60 jobs x ~5.7 h normalbw, 15 gpuvolta jobs x ~3 h. If a sampredict job hits 6 h the next pass resubmits the group and it resumes.",
    "after": "compare with the retired 3 km 2024 map (abs_compare.py on both; IoU matching as in bench/geometry_decision.py), then the other years.",
    "launched": "2026-09-10 08:55 by launcher_2024_9km_v2.sh: pilot slice 0.1542 SU/tile -> 2,512 SU projected; 59 presegment jobs submitted (chunkjobs.txt), SAM+predict groups follow via SP_NODEP sampredict passes every 15 min; then merge/boundary/summary automatically. If the launcher dies: `pgrep -f launcher_2024_9km_v2`; relaunch it (phase A is skipped because pilot_result.txt exists) or drive the stages by hand with the same env (see r['launcher']).",
    "launcher_died_2026-09-10": "Launcher stopped after its 10:48:51 poll. Cause (verified 2026-09-10 16:10): NCI logind KillUserProcesses=yes (/etc/systemd/logind.conf.d/50-nci.conf) kills every process in an SSH session's cgroup scope when the session ends; nohup setsid does not escape it. The VSCode client dropped at 11:01:02; the old VSCode server went silent immediately (never logged its 3 h dispose), and the Claude session that started the launcher died in the same window. PBS jobs unaffected. Relaunched 11:52:50 on gadi-login-04.",
    "submission_ledger": "Every job the launcher submits is recorded on disk: $M/chunkjobs.txt (`p0NN <jobid>`, one line per presegment chunk) and $M/sam/spjobs.txt (`sp_sN <jobid> <timestamp> left=<n>`, one line per SAM+predict group). This is the audit trail the hourly check diffs, and it works regardless of where the launcher process runs.",
    "launcher_risk": "The relaunched launcher (PID 2535046) lives in session-c261910.scope, the SSH session VSCode opened at 11:25:13. It has survived 9 VSCode extension-host SIGKILLs today (those only kill/orphan the Claude session, which is what claude.ai shows as 'archived'), but it WILL die if that SSH session ends, and so will any Claude watchdog in the same scope. If the launcher log's last line is old: relaunch it (idempotent; phase A is skipped).",
    "measured_cost_2026-09-10_19:23": "First 7 of 15 SAM+predict groups finished, all exit 0, SAM rc=0, 0 failed batches. 1,064-tile groups: 125.4-144.7 SU each, mean 137.6 SU (0.1293 SU/tile vs pilot 0.1248), wall 3.5-4.0 h of the 6 h limit, 11.7-13.6 s/tile. sp_s0 (801 tiles) 117.6 SU. Presegment 405.81 logged + ~8.4 for p006 (epilogue missing; PBS history exit 0, 06:43:48). Projected year ~2,500 SU before merge/boundary/summary (pilot projection 2,512; halt gate 4,000). The ~2,290 quoted in the 15:07-18:23 status checks undercounted (14 groups instead of 15, and the pilot's rate). Last groups (sp_s1, sp_s14) due ~21:10-21:30; the launcher then submits merge -> boundary -> summary itself.",
    "sampredict_complete_2026-09-10": "All 15 SAM+predict groups finished by ~21:06, every one exit 0, SAM rc=0, 0 failed batches; 15,968/15,968 composited, segmented and predicted. SAM+predict 2,078.48 SU (1,064-tile groups 125.4-158.3 SU, wall 3.5-4.4 h of 6 h). Year so far 2,533.87 SU = presegment 405.81 + ~8.41 (p006) + pilot 41.17 + sp 2,078.48; pilot projection was 2,512.",
    "merge_2026-09-10": "Launcher submitted merge itself: merge_nat 178687633 (normal, 1 cpu, 8 GB, 5 h), queued 21:22:31. Next it submits boundary once $M/national_2024_crops.gpkg exists and no merge_nat job is queued, then runs summary (-> $M/summary_2024.txt) and logs RUN COMPLETE.",
    "relaunch_hazard_after_merge": "DO NOT relaunch the launcher while a merge_nat or boundary_n job is queued or running. `merged`/`bounded` are plain script variables, so a relaunched launcher resubmits `run_national.sh merge` at once; that subcommand has no live-job or existing-output guard (run_national.sh:774-797), and merge_national.pbs starts with `rm -f $OUT` then appends into it (lines 33-41). A second merge beside a live one deletes/corrupts national_2024_crops.gpkg; a merge during a live boundary job pulls boundary's input away. Once no such job is live, relaunching is safe: it costs one redundant merge rebuild (<=20 SU) and then proceeds to boundary and summary correctly.",
    "state_2026-09-10_22:40": "SAM+predict complete (15,968/15,968). First merge 178687633 FAILED (merged 0 chunks: run_national.sh merge linked outputs as sp<k>_p_<batch>.gpkg, which the p*.gpkg glob never matched). Fixed (links now p_sp<k>_<batch>.gpkg, merge_national.pbs stops on zero input). Launcher RETIRED by hand at 22:10:34 -- do not relaunch. Re-merge 178692909 OK (exit 0): 1,238,529 polygons = sum of the 3,993 batch files, fid dense. 15,948 '(stub, poly_idx) duplicates' are NULL poly_idx on abstained rows, not real duplicates. Running: boundary 178694520 (-> national_2024_crops_merged.gpkg), abs_9km_pre 178693824 (ABS on the pre-boundary predictions, like-for-like with the 3 km report). Every hand-submitted job is in $M/manual_jobs.txt.",
    "next_after_boundary": "(1) `M=$M YEAR=2024 ./run_national.sh classified` -> national_2024_crops_merged_classified.gpkg (good polygons). (2) qsub -q normalbw -l ncpus=4 -l mem=32GB -v YEAR=2024,M=$M,PRED=$M/national_2024_crops_merged.gpkg,OUT=<repo>/output/ABS_COMPARISON_NATIONAL_2024_9km.md abs_national.pbs (headline ABS on the de-duplicated map). (3) qsub -v M=$M,YEAR=2024 compare_national.pbs (centroids, WorldCereal, NLUM, trial-site retention, 3 km vs 9 km). (4) RUN=$M GPKG=$M/national_2024_crops_merged.gpkg NAME=paddock_species_national_2024_9km_crops ./upload_national_crops_gee.sh, then point gee/visualise_national_crops.js FOLDER at the new folder. (5) fill the remaining XXX in output/manuscript/sections and run assemble.py. (6) commit (dead-code removal, fixes, new scripts, manuscript).",
    "state_2026-09-10_23:20": "BOUNDARY DONE (178694520, exit 0, 2.94 SU): $M/national_2024_crops_merged.gpkg, 1,158,824 polygons, fid 1..1,307,224 (148,400 gaps from deleted duplicates; upload script now shards on max(fid)). Products: _classified.gpkg = every polygon with a predicted class (462,028, the headline and like-for-like with the 3 km release) and _good.gpkg = strict mask, abstain_reason empty (356,401). The 105,627 no_crop_shape polygons carry a class: the run predicts every class (--shape-gate-skip-classes Canola Cereal Legume) and only flags the shape-gate failure (PROJ_NOTES 2026-09-07 (2a)); the manuscript's rejected-gate framing counts them as classified. abs_compare.py and compare_maps.py take --count-as-classified no_crop_shape for that. Running: cmp_nat 178699510, abs_9km 178699512 (headline), abs_9km_st 178699513 (strict), abs_9km_pre 178699514 (pre-boundary, like-for-like with the 3 km report); GEE upload on the login node (log $M/gee_upload.log). Commits so far: 9bcaffb (dead code + merge fix + comparisons), 996bed0 (upload fid fix).",
    "complete_2026-09-11": "2024 national map at 9 km COMPLETE. Final: $M/national_2024_crops_merged.gpkg (1,158,824 polygons). _classified.gpkg (462,028, every predicted class, the manuscript headline) and _good.gpkg (356,401, strict mask). All comparisons done: output/NATIONAL_2024_9KM_RESULTS.md (ABS r 0.77 / MAE 4.1 / over-call 1.18x on 151 SA2s; 3 km vs 9 km class agreement 84.0%; WorldCereal 75.3%; NLUM; trial retention 76.3%). Map cost ~2,538 SU. Manuscript output/manuscript filled from the re-run, [CHECK] markers on 4 changed claims, 3 TEMPLATE sections (05/06/07_*_multiyear_TEMPLATE.md). GEE folder paddock_species_national_2024_9km_crops: 20/20 shards landed and public (2026-09-11 04:53). p010's first ingestion stalled server-side for 5 h with ordinary data (76.8 MB, max geometry 21 KB); cancelled and re-ingested from the kept staging blob, it landed in 22 min. Staging blobs deleted.",
    "next": "(1) review the [CHECK] markers and the regional XXX; (2) regenerate Figs 2, 3, 4, 5a, 7a, 8; (3) the other years at 9 km (2023, 2022, then 2017-2021, 2025): grid, chunks, presegment, sampredict, merge, boundary, overlaps, classified, then abs_national.pbs (PRED=$M/national_${YEAR}_crops_final.gpkg, ABS_EXTRA='--count-as-classified no_crop_shape') and compare_national.pbs.",
    "decision_2026-09-11": "User: stay with the 9 km tiling for every remaining year (it looks better than 3 km, though each tiling misses some good polygons the other finds and picks up some bad ones; combining tile sizes and/or offsets is a possible later experiment, not now). User also asked for a final stage merging every polygon group that still overlaps after `boundary`, taking crop type and yield from the largest member: `run_national.sh overlaps` (merge_overlaps.py, overlaps_national.pbs) -> national_${YEAR}_crops_final.gpkg; `classified` now reads that file."
  },
  "note_2024_9km": "national2024_9km is the 9 km re-run of 2024 (adopted model, final pipeline); national2024 (3 km) is the retired map kept for comparison."
}