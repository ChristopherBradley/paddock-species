I want to create a map of cropland species across Australian regions, for the purpose of controlling the species type when analysing the effects of tree shelter on productivity later. But I think species mapping can be it's own intermediate project. 

I have a large dataset of GRDC trial sites with the lat, lon, crop type, planting and harvest dates, and yield across Australia. Generally the surrounding paddock should have the same species type as the trial, although sometimes the trial has multiple species close together, so we need to be careful with this. This data is sensitive (I've signed an NDA), so cannot be committed. Just don't make any commits in general, because I like to read through all code produced and make the commits myself. 

I want to make the crop species predictions using Sentinel-2 imagery (and maybe Sentinel-1 or other data sources if you think it will significantly help predictions based from the literature). I have a Google Earth Engine service account that I can give you access to if you want, but just a free researcher access with a fairly small monthly compute allocation (I don't remember how much but can check if you'd like). I also have access to NCI gadi, including the v10 and ka08 projects which means we can directly access the DEA datacube on NCI. I have some demonstration scripts to show this download process that I can share with you if helpful. I'm part of a project on NCI gadi with ~200KSU/quarter allocation, and I'm willing to put up to 10KSU towards this task. I can also re-launch you directly on gadi via the VSCode remote, instead of getting you to ssh from my local computer if that's helpful. 

I've also placed in the repo the Australia NLUM 250m crop species data, which I think is the current best similar dataset that's available (should search the literature to look for better alternatives). I imagine worldcereal could be helpful as a comparison dataset, and maybe fields of the world could be useful for paddock boundaries. The methodology of worldcereal could be useful too, just trained on a local dataset. I believe canola could be the easiest starting point, since the Canola Flower Index described here is very obvious in heatmaps (based on timing and intensity).
https://github.com/johnburley3000/PaddockTS/blob/main/Code/indices_etc/indices.py

