# Hyperparameter tuning of the crop-group classifier

Generated by `src/paddocks/tune_hyperparams.py` on 2026-09-12. Aggregate only: no trial codes.

- matrix: 2194 trials, 153 features, 3 classes
- grid: 288 configurations over `learning_rate` [0.03, 0.06, 0.1, 0.2], `max_iter` [100, 200, 400, 800], `max_leaf_nodes` [15, 31, 63], `min_samples_leaf` [10, 20, 40], `l2_regularization` [0.0, 1.0]
- fixed: `class_weight='balanced'`, everything not in the grid at its scikit-learn default
- the adopted model is **not** at scikit-learn defaults: `train_species.py` sets `max_iter=400, learning_rate=0.06`
- runtime 4817s

## Temporal split (train <=2022, test 2023-24)

The grid was searched by 5-fold GroupKFold on `site` **inside the training rows only**; the winner was refit on all training rows and scored once on the fixed 543-row test set.

| configuration | test macro F1 | balanced acc. | seed-invariant |
|---|---|---|---|
| adopted (max_iter 400, lr 0.06) | **0.8904** | 0.8971 | yes |
| scikit-learn defaults | **0.8922** | 0.8983 | yes |
| tuned | **0.8777** | 0.8788 | yes |

- tuned minus adopted: **-0.0126** macro F1
- best configuration found: `{"l2_regularization": 1.0, "learning_rate": 0.2, "max_iter": 400, "max_leaf_nodes": 15, "min_samples_leaf": 10}` (inner CV 0.8211)

`HistGradientBoostingClassifier` is deterministic on 2,194 rows (it subsamples only when binning >200k rows, and early stopping is off), so `random_state` changes nothing and seed spread is exactly zero. The yardstick is therefore the test set, not the seed: a paired bootstrap over the 543 test trials (2000 usable resamples of 2000).

- tuned minus adopted, bootstrap mean **-0.0129**, 95% interval **[-0.0308, +0.0043]**
- the tuned model scores higher on **8%** of resamples
- the interval straddles zero

## Spatial split (nested 5-fold GroupKFold on site)

Every outer fold re-ran the whole grid search on its own training rows (3-fold inner GroupKFold), so no fold's test paddocks influenced the settings used to predict them. Scored on the same fixed 543 rows.

| configuration | spatial macro F1 |
|---|---|
| adopted (max_iter 400, lr 0.06) | **0.8923** |
| tuned (nested) | **0.8948** |

- tuned minus adopted: **+0.0025** macro F1

Configuration chosen by each outer fold:

| fold | learning_rate | max_iter | max_leaf_nodes | min_samples_leaf | l2_regularization |
|---|---|---|---|---|---|
| 1 | 0.1 | 400 | 63 | 20 | 0.0 |
| 2 | 0.2 | 800 | 63 | 20 | 0.0 |
| 3 | 0.06 | 100 | 15 | 20 | 1.0 |
| 4 | 0.1 | 200 | 31 | 40 | 1.0 |
| 5 | 0.06 | 400 | 15 | 40 | 1.0 |

- the outer folds did NOT agree on a single configuration, which is itself evidence that the grid is picking up fold noise rather than a setting the data supports

## Verdict

Tuning is **not worth adopting** on this evidence. Temporal -0.0126 macro F1 (bootstrap 95% interval [-0.0308, +0.0043]) and spatial +0.0025.

The adopted `max_iter=400, learning_rate=0.06` stands, and the national predictions do not need to be rerun.

