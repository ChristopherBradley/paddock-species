[2026-08-29] paper-plan: PAPER_PLAN.md built from BRIEF.md, LIT_REVIEW_REPORT.md, IDEA_REPORT.md, NARRATIVE_REPORT.md, PHENOLOGY_GATE.md, NEXT_STEPS.md, POLYGON_STABILITY.md, ABS_COMPARISON_100km.md, ABS_COMPARISON_100km_shapegate.md — 26 sections + figure/table/citation plans, 14 figures/tables planned (1 pending), 15+ claims in evidence matrix (1 pending: national multi-year), venue=Remote Sensing of Environment (backup: Scientific Data). External cross-review (Phase 6) skipped — Codex MCP unavailable, quota-conscious.
[2026-08-29] paper-figure-generate: 4/8 figures generated (Fig05_abs_validation, Fig06_gate_tradeoff, Fig07_multiyear_regional [regional panel only], Fig08_yield_summary). Fig03 reusable as-is from existing confusion_group3_reviewed.png. Still needed: Fig01 (pipeline+example map), Fig02 (study area map), Fig04 (national continent map).
[2026-08-29] User resolved PAPER_PLAN.md §24 Open Issues #1-#4: journal=RSE confirmed; Newman & Furbank citation approved (signed GRDC agreement, but manuscript now requires GRDC pre-submission sign-off before any venue submission — saved to project memory `grdc-publication-agreement.md`); E8 (national multi-year run)=draft now with [PENDING] markers, backfill later; E9 (NLUM/WorldCereal spatial comparison)=approved, national full-10m scope chosen.
[2026-08-30] E9 data acquisition DONE: WorldCereal 2021 (temporarycrops 12GB + wintercereals 7.9GB, 480 tiles each, 10m, national AU extent) downloaded via GEE service account to /g/data/xe2/cb8590/paddock-species-data/raw/worldcereal/ (README.md has full provenance). Discovered during acquisition: WorldCereal has no Canola/Legume class — E9 can only check temporarycrops-extent-vs-our-presence-footprint and wintercereals-vs-Cereal. springcereals confirmed zero AU coverage, maize skipped as irrelevant. NLUM already local. Spatial comparison analysis script not yet written.
[2026-08-30] paper-figure-generate: Fig01 (hero: pipeline schematic + Riverina example map), Fig02 (study area map), Fig04 (national continent map, 3km majority-class grid) generated — output/figures/{Fig01_hero,Fig02_study_area,Fig04_national_map}.{png,pdf} + scripts. Wrote first FIGURE_MANIFEST.md and FIGURE_CAPTIONS.md covering all 8 figures. Found and corrected two PAPER_PLAN.md errors: Fig04 caption said "five abstain reasons" (actual: four) and Fig02's SA2 count is 133 not 132 when re-derived from ABS_COMPARISON_NATIONAL.md's own table (rounding-boundary artifact, noted not resolved). Fig03 still unverified per the plan's own flag — not touched this pass. All 8/8 figures now have a status (7 ready, 1 ready-but-unverified).
[2026-08-30] Fig03 regenerated from output/arms/GROUP3_reviewed.md's raw confusion counts (Fig03_confusion_matrices.png/.pdf) — matches 0.821 temporal / 0.823 spatial macro F1 exactly, resolving PAPER_PLAN's flagged unverified-numbers concern. All 8/8 figures now DONE.
2026-08-30 paper-draft mode=partial venue=RSE words=9237 gaps=8 next=colleague discussion of validation methodology, then either run E8 (national multi-year) or revise based on feedback — see output/manuscript/DRAFT_README.md and REVISION_NOTES.md. Full 10-section draft in output/manuscript/MANUSCRIPT_DRAFT.md; all 8 figures done, E9 (WorldCereal/NLUM) folded into Methods/Results/Discussion/Limitations; 4 flagged citations verified via WebSearch (1 year correction: Lawes et al. 2022, not 2021/2023 as guessed). Every national-multi-year claim marked [PENDING: E8], none fabricated.
[2026-08-30] Found and fixed a real Triticale error while building the eval-sites layer: PAPER_PLAN.md + 2 manuscript sections said Cereal="wheat+barley+oat+triticale" and "the original ten-species scheme" — verified against train_species.py's GROUP dict and nvt_trials_labeled.csv's actual crop values, there is no Triticale in the trial data (9 species: Wheat/Canola/Barley/Chickpea/Oat/Field Pea/Lupin/Faba Bean/Lentil). Fixed to "wheat+barley+oat" and "nine-species" in both places.
[2026-08-30] agriwebb_holdout_group3.py: found the old AgriWebb crop-paddock holdout reports (HOLDOUT_AGRIWEBB.md + _common86/_sam/_padcap variants, in the sibling ../agriwebb repo) all scored the RETIRED 4-class group4 model (39.4% of these same paddocks called Grazing — the finding that motivated dropping Grazing). Re-ran the same 104 AgriWebb crop paddock-years (74 Cereal/25 Canola/5 Legume, farmer-recorded, never trained on) against the CURRENT shipped group3 model (the identical temporal-transfer model verified in eval_predictions.py) — macro F1 0.5917, stable across seeds 0-4 (zero variance, same HGB mechanism as the NVT check). Per-class: Cereal F1 0.85, Canola F1 0.64, Legume F1 0.29 (n=5, too small to trust alone). Found and fixed a real extraction-glob bug mid-task: the AgriWebb ts_idx dir splits across r*/val_*/neg_* prefixes and an initial r*-only glob silently dropped 33 of 104 sites (71/104) before the val_* batch was found and added. Committable report: output/HOLDOUT_AGRIWEBB_group3.md. SENSITIVE per-site predictions: derived/agriwebb/val/agriwebb_group3_predictions_SENSITIVE.csv.
[2026-08-30] output/PIPELINE_ARCHITECTURE_AND_TILING.md extended: (1) index-choice history — confirmed via source search that EVI/SAVI/GNDVI (used by Sharma 2026, Al-Shammari 2024, both already in LIT_REVIEW_REPORT.md) were never tried; NDVI/NDYI/CFI trace to PaddockTS's indices.py per BRIEF.md, not a literature search; bands-vs-indices-vs-combined WAS tested at 9-species scope (SPECIES_MODEL_*.md, all ~0.29-0.32 macro F1, statistically indistinguishable). (2) Boundary-split quantification: reconstructed the exact 3km tile grid from aois.csv (origin consistent to 0.03m across 99,465 tiles), checked all 1.3M national polygon bboxes via GDAL SQLite ST_MinX/MaxX/MinY/MaxY (24s) — 6.99% within 10m of a grid line, 10.25% within 15m; found a confirmed real Legume/Cereal split pair (11.67ha+95.01ha) near the Riverina test region for the overlap experiment. (3) Pointed to the pre-existing PADDOCK_BOUNDARY_BENCHMARK.md FTW-vs-SAMGeo figure (already exists, not regenerated) — flagged as SENSITIVE (real trial imagery+points). (4) Overlap experiment: PBS job 177786687 submitted (gpuvolta, single 4km AOI centred on the confirmed split, same production SAM settings) to test whether a boundary-crossing read window merges the paddock into one polygon — background poll running, result pending.
[2026-08-30] Added §2.2 to PIPELINE_ARCHITECTURE_AND_TILING.md: confirmed the "paddock-median" feature is a genuine median-of-medians, verified identical in extract_paddock.py (training) and predict_tile.py (prediction) — spatial median over 10m-eroded, cloud-masked paddock pixels per observation date (indices computed per-pixel before medianing, not from median reflectance), then temporal median across dates within each 20-day DOY bin. Both stages use median, never mean; unused _pad_mean/_win_mean columns are also extracted but not fed to the model. User confirmed: follow up on vegetation-index literature comparison (EVI/SAVI/etc., §2.1) after the overlap experiment (job 177786687, still queued) completes.
[2026-08-30] AgriWebb discussion deliverables: (1) output/discussion_figures/agriwebb_group3_vs_group4.png/.pdf — group4-vs-group3 F1 comparison + the 39.4%-vs-0%-called-Grazing panel, explicitly marked NOT a manuscript figure (user does not want AgriWebb in the paper; may revisit after talking to the AgriWebb team). (2) derived/agriwebb/val/agriwebb_eval_sites_SENSITIVE.gpkg — per-site true/group4-pred/group3-pred for the 104 holdout sites; 29/104 flip from wrong to correct dropping Grazing (27 of those specifically mis-called Grazing), only 2/104 newly wrong. Verified group4_map.joblib reproduces HOLDOUT_AGRIWEBB.md's confusion matrix exactly before trusting its per-site predictions.
[2026-08-30] eval_predictions.py: re-ran the reviewed arm's temporal + spatial (5-fold GroupKFold out-of-fold) scoring across seeds 0-4, reusing train_species.py's exact feature-building code. Verified exact match to GROUP3_reviewed.md (confusion matrices identical, macro F1 0.8211/0.8229 vs reported 0.821/0.823). Seed variance is exactly zero across all 5 seeds (HistGradientBoostingClassifier's random_state only affects feature-binning above 10,000 rows; this dataset has 2,194) — consistent with a prior documented finding on a different model (S1_MODEL.md), not a new anomaly. Aggregate stability report: output/EVAL_SEED_STABILITY.md (committable, no TrialCodes). Per-site true/predicted/correct joined into the SENSITIVE eval_sites gpkg (derived/keep_arms/, gitignored): 440/543 correct on both splits, 61 wrong on both, 22/20 split-specific errors.
[2026-08-30] Built colleague-discussion GIS deliverables for the 100km Riverina regional run, all at /scratch/xe2/cb8590/paddock-species-data/derived/map100/: 9 per-year gpkgs (by_year/map100_2017.gpkg...map100_2025.gpkg, rebuilt from the 30 arbitrary FID-shard pred/ chunks which each straddle 1-2 years — verified per-year classified-share matches ABS_COMPARISON_100km.md exactly), plus eval_locations_SA2.gpkg (the 5 public ABS SA2s used for the 100km ABS validation + the 100km box, 2 layers). consensus_crops.gpkg (pre-existing) is the "10th" consensus file. A separate SENSITIVE artifact (site-level GRDC trial locations for the classifier's 543-site eval set) was also built per explicit user request — see derived/keep_arms/, gitignored, not detailed here per NDA handling (CLAUDE.md Sensitive Data section).
[2026-08-30] E9 comparison analysis DONE: src/paddocks/worldcereal_compare.py and nlum_compare.py, 250k-point stratified sample of national centroids, results in output/WORLDCEREAL_COMPARISON.md and output/NLUM_COMPARISON.md. WorldCereal: 73.1% presence agreement (symmetric ~73% both directions), Cereal-vs-wintercereals only 49.6% agreement. NLUM: built an enrichment-ratio metric to correct for its cereal layer running high everywhere (raw cross-commodity probabilities aren't comparable) — Canola points 6.4x abstained-baseline oilseed probability, Legume 1.6x; grazing probability 33.8% at abstained points vs 10.8% at classified points, corroborating NONCROP_CLASS.md. Caught and fixed a real bug mid-session: worldcereal_compare.py's first run mislabeled a grand-total-normalised table as "row-normalised" — reran after the fix, headline 73.1%/49.6% numbers were computed independently of the buggy function so did not change, but the per-row breakdown did (now correct). Not yet done: folding these into a manuscript figure/table (paper-draft-stage work, not E9 itself).
[2026-08-31] Vegetation-index sweep (INDEX_ARCHITECTURE_SWEEP.md, following up 2026-08-30's flagged literature gap) added 13 new index families to train_species.py (EVI/EVI2/SAVI/GNDVI/CIre round 1; NDRE2/Vi2/Vi3/VDVI/VCI/EVI2_sharma from Sharma et al. 2026's Table S2 round 2, user-supplied after MDPI blocked automated fetch) and built a 19-index candidate model: macro F1 0.892 temporal / 0.884 spatial vs. shipped 0.821/0.823 (GROUP3_MODEL_19index.md, _VALIDATION.md). INDEPENDENT_REVIEW_19index.md (generator-evaluator separation, CLAUDE.md) then found three problems and returned DO NOT ADOPT — NEEDS MORE EVIDENCE: (1) a `family()` bug let 10 raw-band columns leak past `--bands-drop-raw` in every "indices-only" arm to date; (2) the "bugfix not new indices" causal story was confounded — the isolation arm used to argue it actually bundled 5 never-shipped index families and a different CFI computation; (3) an undisclosed regression on canola-at-5%-FPR (89.0%→85.8%), the metric the codebase's own comments call the actual operating point, with no comparison table in the validation report. All three addressed same day: (1) fixed `family()` at the source (two attempts — the first naive fix broke the opposite direction, silently un-matching band families that end in a digit like `nir_1`); (2) added `--bands-keep-families` and ran the review's own recommended experiment — Sharma's 6 indices joined directly onto the shipped model's own 3 indices, computed the shipped model's own way; (3) turned out moot — the clean candidate has NO canola regression (89.0%, exact match) while matching or beating the messier 19-index model's macro F1 gain (0.890/0.892 vs 0.892/0.884) with less than half the features (153 vs 323/333) and none of the confounds. Recommended candidate + full resolution writeup: output/GROUP3_MODEL_shipped_plus_sharma6_VALIDATION.md. The two original 19-index reports were marked SUPERSEDED in place (banner added, historical content preserved) rather than rewritten, since they're what the independent review actually reviewed. Not yet done: an independent review of the new shipped3+sharma6 candidate itself — this pass and the fixes it validates came from the same process, so per CLAUDE.md's generator-evaluator rule a fresh independent check is the natural next step before treating it as ready to ship (flagged explicitly in the new report's Status line).
[2026-08-31] experiment-design (user request, following fj7 project access landing): output/EXPERIMENT_PLAN.md + EXPERIMENT_TRACKER.md built for three asks — re-test S1's classifier gain against the CURRENT shipped+sharma6 model (not the retired 3-index baseline S1_MODEL.md measured against), re-run Presto with all 5 data sources unmasked (S1+ERA5+SRTM, the 2 groups presto_s1.pbs left masked), and benchmark fj7 processing cost vs the S2-only national baseline (6,622.6 SU, NATIONAL_2024_RUN.md) and the previously-costed MPC S1 route (7,957 SU nationally, 91.5 SU at NVT-trial-pilot scale). Checked fj7 directly this session (ls/find on /g/data/fj7) rather than assuming SENTINEL1_ACCESS.md's open questions were resolved by access alone — found two real, previously-undocumented blockers: (1) fj7 holds only raw SLC/GRDH/OCN, sharded by opaque UUID, and the one thing that would make scenes findable by AOI+date — /g/data/fj7/filelists/*.csv.gz — is -rw------- to its owner (mp2758), unreadable to this account (confirmed via a direct zcat, permission denied); (2) no RTC/terrain-correction toolchain (pyroSAR/SNAP/GAMMA) is installed anywhere in the project env or visible via `module avail`. Both are now Milestone 0 in the plan, gating the compute-benchmark and national-scale blocks; the classifier re-test (M2) and Presto ERA5/SRTM sourcing (M3) need neither fj7 nor SU spend and can run immediately, so the plan sequences those first. No SU spent this session — planning only.
[2026-08-31] s1_vs_latest.pbs (job 177862351, 3.06 SU, user-requested re-test): S1 against the CURRENT best classifier (shipped+sharma6) and the CURRENT shipped yield model (cereal_yield.joblib, pooled Wheat+Barley+Oat) -- neither had been tested with S1 before (every prior S1 number used the retired 3-index classifier; the old YIELD_optical_s1.md tested species separately, not pooled). Result reverses the classifier story and confirms a new one for yield. Classifier: S1 REGRESSES shipped+sharma6 (-0.023 temporal / -0.018 spatial macro F1), landing specifically on Legume F1 (0.82->0.77) -- the exact class S1 used to fix against the old baseline, now made worse because Sharma6's ndre2 already fixed that confusion (permutation importance +0.134 without S1, collapsing to +0.018 with it, every other family turning negative -- classic feature dilution on 1,636 training rows against 204 features). Yield: S1 clearly HELPS cereal_yield.joblib, +0.092 R2 on the operational temporal+year/state bar (0.419->0.511), positive across every arm. Full writeup output/S1_VS_LATEST_MODEL.md; EXPERIMENT_PLAN.md and EXPERIMENT_TRACKER.md updated in place (Block 2/5 re-scoped, C1 marked REFUTED, new claim C1y added for yield). Practical upshot for operationalising S1 at scale: the classifier case for national S1 is closed (negative, not just weak), the yield case is open and now the only one worth costing against fj7. User is taking this to a colleague discussion before deciding on scale-up.
[2026-08-31] canola_yield_sharma6.pbs (job 177871743, user question "is the latest model better at canola yield than just state"): extended train_yield.py with --bands/--bands-keep-families (mirroring train_species.py's identical flag -- train_yield.py previously only took --indices/--s1, this is the first time any yield model in this project has used band-derived indices at all). Answer: NO, worse. Sharma6 collapses temporal R2 (satellite+year/state) from 0.206 (old 3-index model) to 0.012, and nearly erases the spatial edge over year+state baseline (0.376 -> 0.322 vs baseline 0.316). Sharma6 was validated for classification only; this is its first regression test and it regresses. YIELD_FEASIBILITY.md updated in place with a banner; new reports output/CANOLA_YIELD_indices_only.md, CANOLA_YIELD_shipped_sharma6.md. Also answered from existing docs, no new runs: (1) consensus paddock-matching rule (polygon_stability.py: IoU>=0.5 across years, union-find identity, kept if found in >=5/9 years, outline taken from the year the paddock was largest); (2) consensus can't fix the 1.47x/1.59x area over-call -- it's a systematic presence-gate bias ("everywhere", 1.35-1.77x across states), not per-year noise a multi-year vote would average out, and the consensus layer doesn't arbitrate presence at all today; PHENOLOGY_GATE.md's two untried refinements remain the live lever, not consensus; (3) shifting the tile grid per year IS a well-targeted idea for the separate, smaller boundary-split problem (~7-10% of polygons, confirmed same fixed grid reused every year via nlum_tiles.py) but the current union-find's IoU>=0.5 threshold would not link a cut fragment to a whole-paddock year (measured example: fragment vs whole IoU ~0.11) -- would need a containment/overlap-coefficient matching rule alongside the grid shift, not the grid shift alone.
[2026-09-03] paper-plan (v1.1, incremental update, not a rebuild): folded five post-draft findings into output/PAPER_PLAN.md — S1 re-tested against current best models (classifier REGRESSES -0.023/-0.018 macro F1, Legume-concentrated; Cereal yield IMPROVES +0.092/+0.022 R2, splitting the original single "S1 helps" story by task), Sharma6 indices regress canola yield (R2 0.206->0.012), and a leading classifier candidate (shipped+sharma6, macro F1 0.890/0.892, no canola@5%FPR regression) disclosed as a NOT-yet-independently-reviewed, NOT-adopted candidate. Updated §0, §5, §10 (added E10-E12), §11, §12, §14 (added Table 7-8), §16, §17, §19, §23, §24 (added Open Issues #8-9), §26. New Open Issue #8 (adopt the candidate classifier before drafting, or report as future work?) explicitly left open for user decision, not resolved. E8 (national multi-year) status updated to IN PROGRESS, 2/9 years done, running in a parallel session. Next: paper-draft to fold these into MANUSCRIPT_DRAFT.md.
2026-09-04 paper-covert venue=Scientific_Data(backup_ESSD) source=reviewed pdf=ok docx=ok gaps=5 -- built output/submission_scidata/ (LaTeX tree, 18-page PDF, DOCX), a SEPARATE package from output/submission/ (RSE). No Scientific Data/ESSD profile exists in this skill; fell back to profiles/generic.yaml (article class, plainnat), same fallback pattern as the RSE package. Section files named after this manuscript's own Data Descriptor structure (Abstract/Background & Summary/Methods/Data Records/Technical Validation/Usage Notes/declarations), not the skill's default research-article template. All sections rendered unnumbered (\section*), matching real Scientific Data typesetting -- correctly departs from the RSE package's numbered convention since this manuscript cross-references by section name, not number. references.bib built from CITATION_GAPS.md's exact 6-citation list (copied, verified entries from the RSE package's .bib, not re-typed). 8 figures reused (same files as RSE package), Fig 6 caption reworded to match this manuscript's Methods-justification framing. Verified in the compiled DOCX text: 0 leak of the excluded 0.890/0.892 candidate model, 6 PENDING markers preserved (E8 x-refs, 2 deposit gaps, Fig 7 national panel). Known real gaps (not drafting defects): no dataset/code DOI, no submit-check-equivalent run yet for this venue, no real publisher LaTeX class (Scientific Data/ESSD) available. Full detail: output/submission_scidata/SUBMISSION_MANIFEST.md.
2026-09-04 paper-review-loop round=1 mode=integrated revision=partial majors=0/2 verdict=almost (Scientific Data manuscript, output/manuscript_scidata/) -- cold-read subagent found 2 majors (Abstract sentence implied WorldCereal/NLUM corroborate canola COMPOSITION specifically, when only a general presence check and a non-composition enrichment ratio exist; central gate-design justification claimed Cereal/Legume recall "essentially unchanged" under the phenology-shape gate while the comparison table left those exact cells blank -- checked against PHENOLOGY_GATE.md, actual deltas are real: Cereal -4.6pts, Legume -4.1pts) plus 4 moderates (unreconciled 2,194-vs-3,439 trial populations; same blank-table root cause; missing Cereal-yield +0.092 R2 S1 number the plan called for; two 100+-word sentences), all fixed. Both critical requested checks PASSED: no leak of the excluded 0.890/0.892 candidate model anywhere (deliberate exclusion per PAPER_PLAN_SCIDATA.md S24 point 6, verified clean); both newly-added findings (3-groups-vs-9-species dual justification, canola-yield-needs-S1) confirmed present and correctly worded from the first draft. Both hard word limits re-verified independently: Abstract 166/170, Background & Summary 579-581/700. All 8 figures correctly referenced, none phantom. words=5976 (+185 from fixes, no padding). Full artifacts in output/manuscript_scidata/ (REVIEW_REPORT.md, MAJOR_ISSUES.md, MINOR_ISSUES.md, CLAIM_RISK_REPORT.md, REVISION_LOG.md, JOURNAL_FIT_NOTES.md, SECTION_REVIEW_NOTES.md, NEXT_LOOP_PRIORITIES.md, REVIEW_LOOP_STATE.json) and output/AUTO_REVIEW.md. Next: no paper-covert-equivalent run yet for this venue -- RSE's output/submission/ package must not be reused (different structure/citation style). Real remaining blockers: dataset+code DOI deposit, GRDC sign-off. This whole step will re-run once E8 completes or RSE Open Issue #8 resolves.
2026-09-04 paper-draft mode=partial venue=Scientific_Data(backup_ESSD) words=5443 gaps=2 next=review-loop-equivalent pass, then dataset/code deposit for a real DOI -- drafted output/manuscript_scidata/ from PAPER_PLAN_SCIDATA.md (separate from output/manuscript/, the RSE draft). Data Descriptor structure: Abstract 166/170 words (hard limit, compliant), Background & Summary 579/700 (hard limit, compliant), Methods/Data Records(new section)/Technical Validation/Usage Notes/Data+Code Availability -- no Introduction/Related Work/Results/Discussion/Conclusion. Reused all 8 existing figures as-is (no regeneration), one caption (Fig 6) reworded away from "rejected fix" narrative per the plan. Reframed per plan S0/S16: phenology-gate rejection, Presto negative result, and S1 task-split become Methods design-justification, not Discussion findings. Deliberate scoping decision (plan S24 point 6): the leading classifier candidate (0.890/0.892, Open Issue #8) does NOT appear anywhere in this manuscript, unlike the RSE draft which discloses it -- verified by direct grep, clean. Also verified clean: no "we show/we find" framing anywhere (Scientific Data's abstract rule extended manuscript-wide). Both newly-confirmed findings (3-groups-vs-9-species dual justification, canola-yield-needs-S1) included from the start, not stale. Two real blockers to actual submission (not drafting): no dataset DOI, no code deposit -- both marked [PENDING] in Data/Code Availability. E8 gated exactly as the RSE draft is. Full detail: output/manuscript_scidata/DRAFT_README.md.
2026-09-04 canola_yield_s1.pbs (job 178174639, user-requested re-test before writing anything into a manuscript): re-tested canola yield with S1 against the current row-matched (keep_s1both) population -- YIELD_FEASIBILITY.md's "do not ship canola yield until S1 is available" verdict rested on a 2026-08-25-or-earlier number (YIELD_MODEL.md/YIELD_optical_s1.md) that predated keep_s1both's row-discipline fix and had never been re-run. CONFIRMED, not reversed: spatial R2 0.271 optical-only (still below the 0.290 year+state baseline) -> 0.371 with S1 (clearly above it), near-identical to the original unrepeated test. Unlike the classifier's S1 result, this one held up on re-test. New evidence files output/CANOLA_YIELD_s1ctl.md, output/CANOLA_YIELD_s1.md. Updated in place: RSE manuscript (Methods S4.2 -- added the macro-F1-0.38-vs-0.82 accuracy justification for 3 groups vs 9 species, user-requested, alongside the existing label-conflict justification; Limitations item 5 -- canola yield reframed from "doesn't work" to "needs S1, confirmed on re-test, gated on the same acquisition-cost question as Cereal yield"; Future Work item 7 -- broadened from Cereal-only to both yield models), PAPER_PLAN.md v1.2 (same two fixes + new Claims-Evidence Map row), and PAPER_PLAN_SCIDATA.md (same two fixes, Methods S9 points 2 and 5, Limitations S17). sections_revised/ mirrors updated to match. NOT yet re-synced: output/submission/ (PDF/DOCX built before these edits, now stale by these two content changes -- not rebuilt this pass, flagged to user).
2026-09-04 paper-plan: PAPER_PLAN_SCIDATA.md built (separate file, does not touch PAPER_PLAN.md) -- venue=Scientific Data (backup ESSD), 26 sections, reusing all 8 existing figures (no regeneration) and PAPER_PLAN.md v1.1's evidence base, but genuinely reframed for the Data Descriptor format: Abstract <=170 words/Background & Summary <=700 words (both hard limits, verified live via WebSearch), no "new scientific findings" language, no Discussion section (RSE's phenology-gate/Presto/S1-covariate findings redistributed into Methods as design-justification and Technical Validation as data-quality reporting instead of standalone results). ESSD's Data Description requirements freshly researched (not previously done in this project) -- required Data availability/Code availability sections, no strict word cap, "extensive analysis/interpretation out of scope" (same underlying constraint as Scientific Data's abstract rule, reached differently), same journal WorldCereal was published in. Deliberate scoping decision, flagged not hidden: the leading classifier candidate (Open Issue #8) is excluded entirely from this manuscript (no natural home in a Data Descriptor), unlike the RSE draft which discloses it. New open issues: dataset needs a real DOI deposit before Data Availability can be finalized; E8 and Open Issue #8 both carried through unresolved exactly as in the RSE plan. Expected draft length ~6,600-7,600 words (~60% of RSE manuscript, expected given the format's own tighter budget). Next: paper-draft against this plan (paper-figure-generate can be skipped, figures reused as-is bar one caption reword).
2026-09-04 paper-covert venue=RSE source=reviewed pdf=ok docx=ok gaps=6 -- built output/submission/ (LaTeX tree, 27-page PDF, DOCX) from the post-review-loop MANUSCRIPT_DRAFT.md. Installed pandoc 3.11 via conda (not preinstalled); pdflatex+bibtex used (latexmk/xelatex unavailable). Fallback article class, not real elsarticle -- flagged for re-wrap before actual submission. All [PENDING: E8] markers and the leading-candidate-model disclosure (0.890/0.892, not adopted) verified carried through faithfully into both PDF and DOCX, correctly caveated, not resolved. Stripped manual section numbering so LaTeX auto-numbering matches the manuscript's hundreds of literal "Section N.M" prose cross-references; Abstract and Declarations rendered unnumbered to keep Introduction=1..Conclusion=8 aligned. references.bib synthesized from PAPER_PLAN.md S22/CITATION_GAPS.md, 12 citations converted from prose to \citep/\citet, 0 undefined after build; 6 of 12 entries have an honestly-flagged incomplete author list/DOI (not fabricated). 8/8 figures + all 8 tables carried through. Full detail: output/submission/SUBMISSION_MANIFEST.md. Not submission-ready (needs submit-check, real author list, elsarticle re-wrap) and not final (will re-run once E8 completes / Open Issue #8 resolved, per user's stated plan).
2026-09-04 paper-review-loop round=1 mode=integrated revision=partial majors=0/4 verdict=almost -- cold-read subagent found 4 majors (Tables 2-5 referenced but never built; Table 7/8 numeric mismatch for the same candidate model, unreconciled; PAPER_PLAN.md S16's Legume discussion point missing; Abstract/Results MAE claim missing the required 6.8% floor caveat), all fixed this round without touching E8 or Open Issue #8. Critical requested check PASSED: no leak of the unreviewed 0.890/0.892 candidate model's numbers into Results/Abstract/Introduction anywhere -- confined to Discussion/Limitations/Conclusion, always caveated. Also fixed: 2 Highlights bullets over RSE's 85-char limit, Abstract over word target (now 250), an imprecise "trained on" wording. words=11159. Deferred: Mod3 (Limitations/Future Work section-length balance vs Discussion overlap) -- see NEXT_LOOP_PRIORITIES.md. Full artifacts in output/manuscript/ (REVIEW_REPORT.md, MAJOR_ISSUES.md, MINOR_ISSUES.md, CLAIM_RISK_REPORT.md, REVISION_LOG.md, JOURNAL_FIT_NOTES.md, SECTION_REVIEW_NOTES.md, NEXT_LOOP_PRIORITIES.md, REVIEW_LOOP_STATE.json) and output/AUTO_REVIEW.md. Next: paper-covert this session; this whole step (paper-review-loop) will need to run again once E8 completes or Open Issue #8 is resolved.
2026-09-03 paper-draft mode=partial venue=RSE words=10150 gaps=2 next=paper-review-loop then paper-covert this session; both to be re-run once E8 completes and Open Issue #8 (candidate-model adoption) is resolved -- revision pass against PAPER_PLAN.md v1.1, folded 5 post-draft findings (S1-vs-current-best-models task split, Sharma6-vs-canola-yield regression, leading shipped+sharma6 classifier candidate not adopted) into Discussion (new S6.5, old S6.5 Generalizability renumbered to S6.6) and Limitations/Future Work (item 6 rewritten, item 10 + Table 8 added, 2 new future-work items), plus a third open thread in Conclusion. New Tables 7-8. Abstract/Intro/Related Work/Study Area/Methods/Results untouched by design (secondary findings, not headline claims, per PAPER_PLAN.md S23). All [PENDING: E8] markers untouched. CLAIM_SUPPORT_MAP.md and COVERAGE_GAPS.md updated with the new claims; DRAFT_README.md and REVISION_NOTES.md updated with the revision summary.
[2026-08-31] Three follow-ups from the same session. (1) map100/by_year/*.gpkg (9 files) and consensus_crops.gpkg filtered in place to drop abstained content, for easier QGIS comparison -- by_year drops pred-null rows (6,255-10,673/year), consensus drops n_class_years==0 paddocks (1,216 of 22,771). Reversible from untouched ../pred/*.gpkg and ../consensus.gpkg; README.md updated with the change and the reversal path. (2) Prototyped the containment-metric matching rule for polygon_stability.py (Q4 from the consensus/grid-shift discussion): new opt-in --min-containment flag (default off, byte-identical behaviour when omitted), containment(a,b)=intersection/min(area_a,area_b), tracked as a separate best-candidate search from IoU per other-year since the true whole-paddock match is rarely a fragment's best-IoU candidate. Validated against 3 of the 4 real confirmed boundary-split pairs on disk from the overlap experiment (derived/overlap_test/before_pair{2,3,4}.gpkg vs overlap_demo{2,3,4}_filt.gpkg): containment correctly catches the two "absorbed into a surviving compact neighbour" cases IoU misses (IoU 0.115->containment 0.911; IoU 0.177->containment 0.945), stays correctly low on the one case that's genuinely lost to the compactness filter rejecting a blob (IoU 0.025/containment 0.065 -- no matching metric can fix that, it's a different bug), and doesn't regress the cases IoU already handles. Flagged risk, not yet mitigated: containment=1.0 also fires for a genuine paddock subdivision/amalgamation (a real land-use change nested inside its predecessor), which is a different, harder-to-avoid false positive than IoU's own known blind spot. Not yet run against the full 9-year corpus (would need the stability.pbs pipeline and real SU) -- offered as the next step, not run unprompted. (3) Compiled the full 9-index list of the shipped+sharma6 candidate model for the user's colleague update (3 index-file indices: NDVI/NDYI/CFI, output/PIPELINE_ARCHITECTURE_AND_TILING.md §2; + 6 Sharma6 band-derived indices: NDRE2/VI2/VI3/VDVI/VCI/EVI2_sharma, output/INDEX_ARCHITECTURE_SWEEP.md "Round 2", formulas verified against train_species.py's add_indices() directly, not copied from the docs alone). Flagged clearly to the user: this candidate has NOT been independently reviewed yet (PROJ_NOTES 2026-08-31 entry 16's own open item) and is not the literally-deployed group3_map.joblib -- "shipped+sharma6" is this project's working name for the leading candidate, not a promoted production model.
2026-09-04 E8 national multi-year status check + 2022 unstuck. Status: 2023 and 2024 fully done (merged). 2022 was stalled: composites 99.2%, segmented only 69.4%, 0/320 predicted, empty queue. 2017/2019/2020/2021/2025 never started (no aois.csv, `grid` never run); 2018 has no directory at all. Diagnosed the 2022 stall from the SAM job logs directly: 38 of 40 original SAM jobs (8 chunks/~2,488 tiles each, 4h walltime) ran to 04:00-04:02 and were killed mid-chunk -- composites mostly built but segmentation stalled. The existing `repair`/`repair-sam` pair only re-derives tiles missing a COMPOSITE (754 tiles here), not this much larger walltime-killed segmentation gap (~29,669 tiles). Added a new `resam` subcommand to src/paddocks/run_national.sh (after `repredict`, before `merge`): batches tiles that have a composite but no segmentation into small ${RESAM_TILES:-250}-tile jobs instead of 8-chunk ones, relying on samgeo_segment.py's existing idempotency (skips AOIs whose _filt.gpkg already exists) so repeated runs only do what's left; also added it to `status`'s job-pattern match and the usage/header comments. Submitted `YEAR=2022 repair` (754 tiles -> 4 presegment jobs) and `YEAR=2022 resam` (29,669 tiles -> 119 jobs, 53 submitted before hitting the MAXQ=180 queue-ceiling headroom, 66 remaining to resubmit as headroom frees -- queue was already holding 123 unrelated jobs from the sibling `shelterbelts` project). Cumulative logged SU across the whole project: 24,390.7. Flagged to the user before continuing: finishing 2022 plus all 6 not-yet-started years, at 2024's actual measured 6,622.6 SU/year (NATIONAL_2024_RUN.md), projects to ~40,000-45,000 more SU -- well past CLAUDE.md's "up to 50 KSU earmarked for this project." User's explicit decision: run all remaining years back-to-back regardless, no further per-year budget check-ins. Order set: 2022 (finish) -> 2017 -> 2018 -> 2019 -> 2020 -> 2021 -> 2025, each via grid(from 2024 AOIs)/chunks/presegment/sam+resam/predict/merge/summary. Wrote handoff.json (new) with full resume state -- a dropped session should read it first, then re-verify against live `./run_national.sh status`/`qstat` since disk/queue state is always authoritative over the snapshot. Set up an hourly `/loop` (CronCreate job 2ea9560a, fires :17 past the hour, session-only, auto-expires after 7 days -- will need recreating if this run is still going then) to advance the pipeline, log progress here each check, and move to the next year automatically once each one merges cleanly. First check (same session, immediately after setup): segmented rose 69.4%->70.9%, 54/119 resam jobs queued (12 running), `repair-check` clean (4 repair jobs still queued/running, none vanished). Overall PBS queue at 177/180 headroom (mostly unrelated shelterbelts jobs) -- next check should resubmit the remaining resam jobs once headroom frees.
2026-09-04 E8 loop check (cron 2ea9560a, 2nd fire). 2022: segmented 73.6% (up from 70.9%), composites unchanged at 99.2%, 0/320 predicted. `repair-check` clean (4 repair presegment jobs still queued/running, none vanished, none finished yet). Overall PBS queue 169/180 -- re-ran `resam`: 25,344 tiles still unsegmented (down from 29,669), submitted 11 more jobs with available headroom, 91 of the needed 102 still queued behind MAXQ for next check. No genuine failures seen. No stage transition yet -- still filling in segmentation before `predict` can start.
2026-09-04 E8 loop check (cron 2ea9560a, 3rd fire). 2022: segmented jumped to 82.2% (from 73.6%) as the overall PBS queue drained from 169 to 18 jobs (unrelated shelterbelts jobs finished, freeing headroom). `repair-check` confirmed the 4 repair presegment jobs (r000-r003) are genuinely Running, not stalled -- just not finished inside their 6h walltime yet. Re-ran `resam` with the newly available headroom (164 of 180): all remaining 68 jobs submitted in one pass (16,953 tiles) -- resam has no backlog left behind MAXQ. No failures. Still pre-predict.
2026-09-04 E8 loop check (cron 2ea9560a, 4th fire). 2022: segmented 85.5% (from 82.2%). Queue 66/180 (mostly Q, waiting on gpuvolta slots -- normal). `repair-check`: 1 of 4 repair presegment jobs now finished, 3 still running, none vanished; composites still 98711 so repair-sam not started yet (waits for repair presegment). Re-ran `resam`: 13,671 tiles still unsegmented, submitted all 55 needed jobs with ample headroom -- no backlog. No failures. Still pre-predict.
2026-09-04 E8 loop check (cron 2ea9560a, 5th fire). 2022: segmented 93.3% (from 85.5%). Queue 96/180. `repair-check` clean: 1/4 repair presegment jobs done, 3 running at ~3:44 elapsed of 6h walltime, none vanished; repair-sam still pending on those. Re-ran `resam` (slower than usual, ~5 min, filesystem contention from the larger job count, not an error): 5,215 tiles still unsegmented, all 21 needed jobs submitted, no backlog. No failures. Still pre-predict, getting close to segmented~100%.
2026-09-04 E8 loop check (cron 2ea9560a, 6th fire) -- GENUINE FAILURE, stopped advancing 2022. Segmented reached 99.2% (98710/99465, matches composite count exactly -- resam is fully done, nothing left there). But `repair-check` found repair job r003 (PBS 178189147) exited status 1, not still running: its own log shows `presegment: 0 built, 0 skipped, 4 failed` after repeated `psycopg2.OperationalError` retries ending in `FATAL: server login has been failing, cached error: connect timeout (server_login_retry)` against the DEA datacube, and the script's own guard fired: `ABORT: 4/4 AOIs failed (100%), above --max-fail-frac 10%`. Cost 3.50 SU, 0 composites produced. The other 3 repair jobs (r000-r002) were still Running at ~4:44/6h elapsed at check time, outcome unknown. No other job in the last 6h of logs shows the same error signature -- looks isolated, not an obvious broad gadi/DEA outage from what's visible here. resam/segmentation itself is unaffected (no datacube dependency) -- this only blocks the last ~753 composite-missing tiles. Per instructions, stopping here rather than guessing a resubmit; PushNotification sent to user; handoff.json's next_step_for_2022 marked BLOCKED pending user review.
2026-09-04 E8 HARD STOP -- user's assessment: the DEA datacube itself is likely down (not just our own connection pool), possible concern our job load contributed, explicit instruction to stop anything using it. Actions: `qdel`'d the 3 still-running repair jobs (178189142/143/144, r000-r002, all still actively querying the datacube at 5h+ elapsed of their 6h walltime) rather than let them keep hitting a possibly-struggling service; confirmed via `qstat -u cb8590` the queue is now completely empty (no paddock-species jobs, and the unrelated shelterbelts jobs had also finished on their own by this point). Cancelled the hourly cron loop (job 2ea9560a, had completed 6 fires). handoff.json's next_step_for_2022 rewritten as a HARD STOP: nothing that opens a datacube connection (repair/presegment/predict/grid->presegment for a new year) runs again until the user confirms DEA is healthy and says to resume. Segmentation itself (resam, no datacube dependency) is unaffected and sits at 99.2%, matching the composite count exactly -- nothing left for it to do regardless, the only gap is the ~753-1128 composite-missing tiles that are entirely gated on datacube access. E8 is now fully paused, awaiting the user.
2026-09-07 E8 resumed, segmentation-only (user explicit: finish segmentation, do NOT run predict; cap 50 jobs/batch, prefer fewer jobs, target <10h total). Pre-flight datacube health check from the login node (`datacube.Datacube().list_products()`) succeeded but took 57.2s -- normally near-instant, so the pooler may still be under some load, but it is not down. Proceeded cautiously given the small blast radius (753 tiles, 4 jobs). Ran `YEAR=2022 ./run_national.sh repair`: 754 tiles missing a composite -> exactly 4 repair jobs (rp_r000-r003, PBS 178329305-308), all Running immediately (97 headroom). Launched a background orchestration script (`/scratch/.../finish_2022_segmentation.sh`, logging to `/scratch/xe2/cb8590/paddock-species-logs/resume_2022_segmentation.log`) that waits for these 4 jobs, runs `repair-check`, then chains to `repair-sam`/`resam`/`status`.
2026-09-07 E8 repair attempt #1 (this session) FAILED CLEANLY -- a genuinely NEW/different failure from 09-04, caught by reading the job logs directly rather than trusting `repair-check`. All 4 repair jobs ran to completion in minutes (not walltime-killed) and `repair-check` reported "no jobs vanished", but every single AOI in all 4 jobs failed (0/754 built) with `not recognized as a supported file format`. Traced to exactly 2 permanently-corrupted files in the DEA archive: `/g/data/ka08/ga/ga_s2bm_ard_3/54/HWH/2022/01/23/.../..._final_fmask.tif` and the `50/JNR` equivalent, both dated 2022-01-23, both confirmed 0 bytes on disk since Jul 2022 (`stat` confirmed, owner `lpgs`/`ka08`) -- a permanent archive defect, not a connection-pool issue, and the reason this exact 753-754 tile subset has been the persistent 2022 gap since the run began (every tile in this set overlaps one of these two MGRS scenes). This exact failure mode was already documented in `samgeo_segment.py`'s own docstring from an earlier pilot run but never actually mitigated in code. Asked the user how to proceed (AskUserQuestion): patch-and-retry, accept-the-gap, or investigate-further. User chose patch-and-retry, and separately flagged the wasted-compute pattern as worth capturing in memory -- saved as `dea-archive-corrupt-scenes-silently-fail-aoi.md`.
2026-09-07 Fix attempt #1 (hand-rolled, WRONG, caught before wasting a real job batch): added `_load_excluding_corrupt_scenes()` to `samgeo_segment.py`, using `dc.find_datasets()` to filter out the bad date then `dc.load(datasets=filtered, ...)`. Bug: dropped `x=`/`y=`/`crs=` from that second `dc.load` call. Without an explicit window, `dc.load(datasets=...)` reads the FULL ~100km MGRS scene extent, not the 3km AOI -- confirmed via a real 2-tile PBS test job (178334971): OOM (exit 137, SIGKILL) at exactly 4.0GB in 14s, far too fast to be genuine AOI-scale computation. Caught by testing on an actual PBS job with the two known-bad tiles BEFORE resubmitting the full 754-tile batch, not by code review.
2026-09-07 Fix attempt #2 (correct, verified): discovered `datacube` 1.8.17 (confirmed via `inspect.signature`) already has the right built-in parameter -- `dc.load(..., skip_broken_datasets=True)`, "don't break when failing to load a broken dataset" (confirmed via its own docstring). Replaced the hand-rolled function entirely; `build_image()` now just adds this one flag to its existing `dc.load()` call, with the query/windowing completely unchanged. Verified via a second real PBS test job (178335312) against the same 2 known-bad tiles: exit 0, `presegment: 2 built, 0 skipped, 0 failed` (76 scenes for the 54HWH-affected tile, 145 for the 50JNR-affected tile -- the corrupt scene simply dropped out, every other scene in the year used normally). Re-ran `YEAR=2022 ./run_national.sh repair` for the full 754-tile batch (4 jobs again) and relaunched the orchestration script, now with an added composite-count cross-check before it will proceed to `repair-sam` (repair-check alone would not have caught attempt #1's failure -- it only detects vanished jobs, not jobs that ran and built nothing).
2026-09-07 E8 2022 SEGMENTATION COMPLETE. The 4 repaired-composite repair jobs (PBS 178336023-026) finished and pushed composites to 99,465/99,465 (100.0%, confirmed by the orchestration script's composite-count check before it allowed the pipeline to continue). `repair-sam` (1 GPU job, PBS 178340697, ~60 min on gpuvolta) segmented the newly-repaired tiles. A final `resam` (1 tiny job, PBS job for the 1 pre-existing composite-but-unsegmented tile predating this repair) closed the last gap. Final `status`: composites 100.0%, segmented 100.0% (99,464/99,465 -- rounding, not a real hole; `incomplete: 0 chunks below 98%`, `SILENT HOLES: 0`). Total wall time for this whole resume (both repair attempts + the corrupt-scene diagnosis/fix + the successful re-run) was ~3.5h from first health check to completion; the successful re-run alone (submit to finish) was ~2h37m. Total jobs across the whole operation: 4 repair (attempt 1, failed) + 4 repair (attempt 2, succeeded) + 1 repair-sam + 1 resam + 2 small validation test jobs = 12, still well under the user's 50-job/batch cap (no single `repair`/`repair-sam`/`resam` call ever exceeded 4 jobs). predict/merge/summary were NOT run, per explicit instruction -- 2022 is now segmentation-complete only. Next available step (not yet authorized): `predict` for 2022 (until 320/320 chunks), then `merge`/`summary`; after that, the 6 not-yet-started years (2017-2021, 2025) remain, each needing `grid`/`chunks`/`presegment`/`sam`+`resam`/`predict`/`merge`/`summary` -- all of which will now benefit from the `skip_broken_datasets=True` fix if they hit similar archive corruption.
2026-09-07 User reviewed the compiled Scientific Data submission PDF and raised 5 points; asked for questions on the first few before proceeding. Full session covered:
(1) MODEL ADOPTION -- user: use the best candidate (shipped+sharma6, macro F1 0.890/0.892) in production, stop shipping the worse one. Found this is gated by CLAUDE.md's generator-evaluator rule (PAPER_PLAN.md Open Issue #8: candidate "not yet independently reviewed"). Asked user: waive the gate or run a review first? User: run a review first. Launched a fresh, independently-briefed Agent (no exposure to this session's own reasoning) to verify `GROUP3_MODEL_shipped_plus_sharma6_VALIDATION.md`'s claims against raw evidence (job logs, train_species.py's actual current code, formula correctness, train/test row-identity), matching `INDEPENDENT_REVIEW_19index.md`'s rigor -- result pending at `output/INDEPENDENT_REVIEW_shipped_sharma6.md`. Also found: no production joblib for this candidate exists yet -- `train_species.py` (used for all its evaluation) has no model-save path; only `fit_map_model.py` does, and it lacks `--bands-keep-families` (needed to reproduce the exact 153-feature spec) -- adopting this candidate is more than a config pointer swap. Asked user whether to backfill 2023/2024 (already merged on the old model) -- user: yes, backfill, but explicitly DO NOT launch those jobs yet (running another project's compute at scale on gadi concurrently). PAPER_PLAN.md's Open Issue #8 updated to reflect the decision + sequencing; no production code changed for the model swap itself (correctly still blocked on the review).
(2a) PREDICT ALL 3 CLASSES REGARDLESS OF ABSTAIN -- user escalated from "just Cereal" to "all 3 (Canola/Cereal/Legume), mask out later if needed" and asked directly whether there's a disadvantage. Found a real one: `--shape-gate-skip-classes` as it existed FULLY bypassed the shape check for a listed class -- no abstain_reason was ever recorded either, so a skipped class's gate failure left literally no trace to "mask out later" against. Worse, 4 downstream analysis scripts that produced this project's actual validated results (`abs_compare.py`, `filter_sweep.py`, `consensus_layer.py`, `polygon_stability.py`) plus `run_national.sh`'s own `summary` SQL all treat `pred.notna()` as "cleanly classified" -- extending the skip list naively would have made all of them silently start trusting gate-failed polygons as clean. Fixed properly: `predict_tile.py`'s shape-gate block now decouples the two (abstain_reason is always recorded from the RAW shape-check outcome regardless of skip-list membership; only whether `pred` gets nulled is controlled by the skip list), and all 5 downstream consumers updated to key off `abstain_reason == ''` instead of `pred.notna()` (with safe fallbacks where the column might be absent in older exports). `run_national.sh`'s predict command now passes `--shape-gate-skip-classes Canola Cereal Legume` (comment rewritten to explain the new semantics) -- config only, no prediction job launched. `run_map100.sh` (the Riverina 9-year pilot that produced the manuscript's ALREADY-PUBLISHED Fig 7) deliberately left untouched, so those figures stay exactly reproducible as documented.
(2b) ZARR PADDOCK TIME SERIES -- user wants the full raw paddock-median time series (not just DOY-binned classifier features) saved as `.zarr`, modeled on github.com/johnburley3000/paddocktimeseries, for a colleague's downstream use. Found `predict_tile.py`'s `zonal_medians()` already computes almost exactly this as an intermediate (per-polygon, per-scene medians) before `train_species.py`'s `build_features()` collapses it into DOY bins and discards it -- so persisting it is mostly an I/O addition, not new compute, except the 6 Sharma6 indices (currently only implemented to run on already-binned data) need adapting to run per-scene. Asked user which variable set/temporal resolution; user: benchmark the raw-per-scene, project's-own-9-indices option specifically, and implement it only if under 10% extra compute AND storage. Launched a second background Agent to build a real (non-mocked) pilot on a handful of already-composited tiles and measure both ratios honestly -- result pending at `output/ZARR_BENCHMARK.md`.
(3) TILE-BOUNDARY POLYGON MERGING + TILE-SIZE BENCHMARK, (4) WORLDCEREAL/NLUM/ABARES three-way comparison -- user approved both, not yet started this session (turn already covered 1/2/5 in full); scoped in the response but deferred pending the user's next message, partly because (3) implies real PBS compute and the user is currently running another project at scale on gadi.
2026-09-07 Both background follow-ups from this session completed.
INDEPENDENT REVIEW (shipped+sharma6): **VERDICT ADOPT.** Fresh reviewer, no exposure to this session's reasoning, verified against raw evidence rather than the summary report -- full byte-for-byte reproduction of the training run (diffed report + MD5-matched both confusion PNGs), canola@5%FPR "exact match" (89.0%) confirmed at full unrounded precision (0.890323 for both models, 138/155, on independently-verified identical held-out rows, different decision thresholds so not a no-op), the prior candidate's `family()` bug confirmed genuinely fixed and structurally unable to recur (allow-list not drop-list), no raw-band leakage, Sharma et al. 2026 citation and formulas both verified. Two minor caveats, neither blocking: a loosely-worded row-identity citation (cosmetic), and an unacknowledged multiple-comparisons exposure (~20 arms scored against the same fixed 543-row test set before this one was picked) worth closing with a fresh holdout before defending 0.890/0.892 to an external reviewer, but not before adoption. Full report: `output/INDEPENDENT_REVIEW_shipped_sharma6.md`. Acted on immediately (code only, no jobs): `fit_map_model.py` (the actual production-fitting script -- `train_species.py` only evaluates) could not combine `--indices`+`--bands --bands-keep-families`; extended it to do so, mirroring `train_species.py`'s exact multi-source join without touching that already-reviewed file. Verified via a lightweight login-node run (not a PBS job, seconds of compute): `fitted on 2194 paddocks x 153 features` -- matches the reviewed spec exactly. PAPER_PLAN.md Open Issue #8 updated to ADOPT + this status. Remaining, all gated on the user confirming compute is free: fit the real production joblib, update run_national.sh/run_map100.sh's MODEL= path, predict 2022 + re-predict 2023/2024, re-run ABS validation against the new model, optionally the fresh-holdout re-check.
ZARR BENCHMARK: **RECOMMENDATION: do not implement as scoped -- both ratios exceed the user's 10% bar.** Real pilot (not a mock) on 5 already-segmented Riverina tiles, $0 SU (login-node only, no PBS jobs, respecting the "don't launch jobs" constraint). Time: extra compute is 52-135% of baseline predict-stage time per tile (mean 81%, median 60%) -- 5-13.5x over threshold. Storage: 8-24% logical / 26-39% physical (on-disk) for a one-zarr-per-tile design -- 1-4x over threshold. Root cause of the time cost: NOT the Sharma6 math or the zarr write (both under 2% of the extra cost) but an architectural fact -- the shipped classifier only reads 4 of 10 bands, so raw-band persistence needs a second, wider datacube read plus a real zonal-aggregation cost for the other 6 bands. Root cause of the storage overshoot: ~47 fixed metadata files (~25KB) per store, paid once per tile regardless of tile density -- hits sparse tiles hardest. Also surfaced a real data-quality wrinkle worth flagging to the colleague regardless of the decision: 3 of the proposed 9 indices (NDVI/NDYI/CFI) would be median-of-index (matching the classifier) while the other 6 (Sharma6) would be index-of-median (matching how `add_indices()` already treats them) -- an inherited inconsistency, not a benchmark artefact. National extrapolation (labelled estimate, cross-checked against real production SU accounting, not scaled from raw pilot numbers): ~940-1,690 extra SU/year (predict stage alone cost 1,578 SU in 2024) and ~10.5-19.8 GB extra nationally. A consolidated (per-chunk, not per-tile) store design would shrink the storage ratio toward threshold but would NOT fix the time ratio, which is the harder blocker. Full report + proposed schema if the user wants to proceed anyway: `output/ZARR_BENCHMARK.md`. New pilot script `src/paddocks/zarr_pilot_extra.py` -- does not touch production code, not currently used by anything.
2026-09-08 MODEL ADOPTION COMPLETED IN CODE (no jobs launched). While preparing to use the reviewed shipped+sharma6 candidate, found a second blocker beyond fit_map_model.py: `predict_tile.py` read a single `kind`/`value_cols` from the bundle, ran ONE `zonal_medians()` pass and reindexed onto `columns` -- for a two-source model (`@indices` + `@bands` suffixed columns) every column of the missing source would come back NaN, which HGB accepts silently: a whole tile classified from a half-empty matrix, exit 0, plausible GeoPackage, nothing downstream to flag it. Fixed: bundle now carries `sources=[(kind, value_cols), ...]` (fit_map_model.py), predict_tile.py runs one zonal pass per source, suffixes/joins exactly as training did, and keeps unsuffixed per-source frames (`F_raw_plain`) for the yield model and the NDVI gates, which were fitted single-source. Older single-source bundles fall back to `[(kind, value_cols)]`. VERIFIED on the login node (no PBS): (a) regression -- the OLD group3_map.joblib on tile r948_c1194 is byte-identical to the pre-refactor pilot baseline for that tile on pred/abstain_reason/confidence/p_*/yield_tha/ndvi_amp; (b) the combined model on the same tile: `sources [('bands',10),('indices',3)]`, 153 columns, scored polygons n_feat_present min 144 / median 153 / max 153 (the pre-fix failure would show ~51 or ~102), non-degenerate classes (Cereal 18 / Canola 17 / Legume 1), identical gate counts to the old model, yield scored on 18 Cereal polygons via the unsuffixed frame. Then fitted the PRODUCTION joblib on the full 2,194-row reviewed keep set: `derived/models/group3_map_sharma6.joblib` (2.4 MB, 153 features, train_years 2017-2024; the retired `group3_map.joblib` left on disk untouched until 2023/2024 are re-predicted). Flipped every `MODEL=` in `run_national.sh` (with a do-not-revert comment) and `run_map100.sh` (4 occurrences) to the new file, and -- for consistency, since a future re-run should use the full current production config -- also updated run_map100.sh's shape-gate flag to `Canola Cereal Legume` (its published Fig 7 outputs were produced with the OLD model + Canola-only skip; those files are untouched and remain reproducible from git history). Memory saved: `multi-source-model-bundle-silent-nan.md`. STILL NOT DONE, gated on the user confirming compute is free: predict 2022, re-predict+remerge 2023/2024, re-run ABS validation / Results numbers on the new model, optional fresh-holdout check (review caveat b).
2026-09-08 ZARR follow-up question from the user ("does it cost extra compute to store the full time-series of just the bands we already load?"): with the OLD model, predict_tile.py loaded only IDX_BANDS (4 bands) and medianed 3 index planes, so persisting just those would have cost no extra read but an extra 4-plane zonal pass (borderline vs 10%). With the NEW model in production this is moot in the right direction: predict_tile.py now reads ALL_BANDS and runs a 10-band zonal pass as part of the classifier's own baseline, which is exactly the two line items that made up >98% of the benchmark's "extra" cost (read_extra + zonal_bands). The marginal cost of ALSO persisting that already-in-memory (paddock, scene, band/index) array is the zarr write alone -- measured 0.43-1.7 s/tile in ZARR_BENCHMARK.md, i.e. ~0.5-2.6% of the pilot's per-tile baseline or ~2-7% of the amortised production baseline once the new model's larger read is included. This is a derivation from the pilot's already-measured components, not a new measurement, and it clears the user's 10% compute bar. Storage is the remaining gate: the per-tile store design measured 26-39% on disk; a consolidated per-chunk store (one per ~311-tile prediction chunk, proposed in ZARR_BENCHMARK.md §5/§8) is expected to fall toward the few-percent data-payload floor but was NOT measured. Proposed to the user: implement as an opt-in `--zarr-out` in predict_tile.py hooked at the existing zonal pass, measure storage on the first real chunk, keep it only if it clears 10%.
2026-09-08 ZARR SHIPPED DEFAULT-ON (user: "add the --zarr option, but make it opt out rather than opt in"; storage budget "less than 30GB/year is fine"). `predict_tile.py` now writes the raw per-scene, per-paddock medians it already computes as the classifier's own pre-binning intermediate: 10 raw bands + ndvi/ndyi/cfi (median-of-per-pixel-index, from the indices source) + the 6 Sharma et al. 2026 indices (index-of-median, formulas copied verbatim from `add_indices()` via the benchmark's verified `sharma6()`), plus n_clear/n_px so a consumer can tell "cloudy" from "not observed". `--no-zarr` opts out; `--zarr-out` overrides the default path ($M/zarr/<chunk>.zarr). ONE CONSOLIDATED STORE PER CHUNK, not per tile -- ZARR_BENCHMARK.md measured per-tile at 26-39% of baseline storage because each store pays ~47 fixed metadata files regardless of tile density, and 99,465 stores would be ~6.9M files, a Lustre metadata problem independent of bytes; a chunk store pays that once per ~311 tiles, matching how predict already writes one GeoPackage per chunk. Time axis is the union of the chunk's tiles' scene dates (tiles in a chunk are spatially contiguous by design so they share most dates; NaN padding compresses to almost nothing). Joins to the predictions GeoPackage on (stub, poly_idx). The dependency is imported at startup, not mid-loop, so a missing `zarr` fails before the datacube reads are paid for. Storage headroom is not a concern regardless: xe2 scratch has ~33 TiB and ~1.2M inodes free.
2026-09-08 PREDICT RUNBOOK PREPARED, NOT LAUNCHED (user: "Don't do the predict 2022/2023/2024 jobs yet, but prepare them so they're ready to go. We'll do them before we do the segmentation for the other years"). New `src/paddocks/repredict_new_model.sh` with `check` / `archive YEAR` / `go YEAR` / `finish YEAR`. The reason it exists rather than just calling `run_national.sh predict`: that command skips any chunk with a non-empty existing output, so on 2023/2024 -- already predicted and merged under the retired model -- a model swap would be a silent no-op. `archive` moves the old `pred/` to `pred_group3_map_retired/` and renames the merged GeoPackages rather than deleting them, because every classifier number currently in the manuscript and both submission packages came from those files and must stay reproducible until the new run replaces them. `check` ran clean today: model is the two-source 153-feature reviewed bundle; 2022 segmented 100.0% with 0 chunks predicted; 2023 has 320 and 2024 has 374 retired-model chunks awaiting archive. Hit and fixed the documented `find`-under-`pipefail` trap while writing it (find on a not-yet-existing dir exits non-zero even with stderr silenced, and pipefail kills the script) -- same trap run_national.sh's `status` case already dodges with a bare mkdir.
2026-09-08 Committed the last two days' work in 4 commits (user: "Feel free to make commits as you think is appropriate"): 26ba2f5 corrupt-scene fix; 9b21c08 model adoption + gate semantics + zarr; fb0f9ae citation and wording fixes; f23357d review/benchmark reports and run state. Deliberately NOT committed: pre-existing uncommitted edits made before this session (train_yield.py, map_regions.py, abs_national.pbs, merge_national.pbs, manuscript sections 00/04/05/08/10) and the untracked generated submission trees (the corrected references.bib files live inside those and were fixed in place; the tracked sources -- CITATION_GAPS.md, PAPER_PLAN.md 22, 11_references.md -- are correct, so a paper-covert re-run cannot reintroduce the wrong initials). Not pushed.
2026-09-08 Points 3 & 4 STARTED (user: "Yes, start on points 3 & 4"). Two background agents launched: (4) `reference_intercompare.py` -> `output/REFERENCE_INTERCOMPARISON.md` -- WorldCereal vs NLUM spatially (aggregated to NLUM's 250 m cells, cropping-zone restricted, NLUM-threshold sensitivity table) and each vs ABS/ABARES sown area at state level for 2021, assembled into the full pairwise disagreement matrix to state a reference-noise floor per class; at most one modest CPU job allowed. (3a) `merge_tile_boundaries.py` -> `output/TILE_BOUNDARY_MERGE.md` + figures under `output/figures/tile_boundary_merge/` -- POST-HOC merging on existing output (PIPELINE_ARCHITECTURE_AND_TILING.md §6 already showed a wider read window does NOT fix splits), explicit conservative rule set (shared-edge fraction, union must pass the 300 ha / compactness 8 filters, class reconciliation by area-weighted p_*), Riverina 60 km box first with the 4 confirmed split pairs as known-answer cases, national pass as at most one modest CPU job, before/after QA figures; no GPU jobs. (3b) tile-size benchmark NOT launched -- needs GPU (SAM) jobs; design drafted for the user's go-ahead: reuse TILE_SIZE_BENCHMARK.md's 4 x 9 km 2021 footprints, arms 3 km (existing, 114 prompts/km2) / 4.5 km (16 tiles) / 9 km (4 tiles) with `points_per_side` chosen per arm to hold prompt density at ~114/km2 (the doc's own finding: polygon differences are driven by prompt density per 512-px window, not tile size per se), scoring polygons/km2, median ha, boundary-touching %, IoU agreement vs the 3 km baseline, and SU/km2; ~20 SAM tiles, est. 1-2 GPU-hours (~40-70 SU).
(5) LANGUAGE + CITATIONS -- fixed a real citation bug: `references.bib` in BOTH `output/submission/` and `output/submission_scidata/` had WRONG author initials AND a paraphrased (wrong) title for both Sharma et al. 2026 and Al-Shammari et al. 2024, despite the .bib's own honesty note flagging them as "not re-verified" -- verified via Crossref (authoritative), corrected both files and `output/manuscript/CITATION_GAPS.md`; `LIT_REVIEW_REPORT.md`'s entries (read from the source PDFs, not guessed) turned out to already be correct and are now what the .bib matches. Removed "hand-engineered"/"hand-built" and the emphasis use of "entirely" from all 7 `output/manuscript/sections/*.md` files per user's explicit wording feedback (kept factual/quantifying uses like "concentrated almost entirely in Cereal" -- those aren't emphasis, they're describing a real degree). NOTE: these phrases do NOT appear anywhere in the actual SciData LaTeX/PDF the user said they read (`output/submission_scidata/`) -- they exist only in the RSE package (`output/submission/latex/sections/*.tex`, also fixed) and the shared `.md` source, so the user was likely looking at the RSE PDF while calling it "scidata," or reading the .md source directly -- flagged to user, not silently assumed. `MANUSCRIPT_DRAFT.md` and both compiled PDFs are now stale relative to the fixed `.md`/`.tex` source and need a `paper-draft`/`paper-covert` re-run to pick up the wording fix; not done automatically. Explained `paper-review-loop --mode language` to the user (scopes a review+revise pass to prose/wording only, same generator-evaluator structure as the other modes) rather than invoking it, since the ask was 2 specific words plus general guidance, not a full systematic sweep.
2026-09-08 TILE-BOUNDARY MERGE + VISUAL QA DONE (user: "do those 3 steps ... use your image recognition skills to debug and find a good way to remove any artifacts along tile boundaries ... with some quantitative demonstration"). Everything on the login node, $0 SU, no PBS job launched (the 13 queued `pred_2021` jobs are the shelterbelts project's). Deliverables: `output/TILE_BOUNDARY_MERGE.md` (generated by `src/paddocks/tile_boundary_report.py` from the run JSONs so the numbers are the files'), 14 figures under `output/figures/tile_boundary_merge/`, and six scripts in `src/paddocks/`: `merge_tile_boundaries.py` (the fix; D1-D3 ownership/coverage/conflict, M1 union, NEW M2 sliver clip to the owning tile, NEW `raster_cut_m` + `clip_ha` columns), `boundary_seam_audit.py` (independent before/after measurement), `boundary_qa_figures.py` ([composite | before | after] windows with the lattice AND each tile's real raster footprint drawn), `cut_continuity.py` (image test: does the field continue past a cut?), `cross_year_check.py`, `repair_cuts_multiyear.py` (validate/repair from a donor view). Plus `boundary_national.pbs` + `run_national.sh boundary` (1 CPU / 32 GB / 4 h, NOT run nationally -- belongs after each year's `merge`; sized from 249 MB / 15 s on the 10k-polygon box). PIPELINE_ARCHITECTURE_AND_TILING.md sec 4 corrected in place (sec 5/6 annotated, not rewritten).
KEY FINDING (the "debug"): the earlier "tiles do not overlap" claim was wrong -- read off the dc.load query, missing `output_crs=EPSG:6933`: every raster is the 6933 bbox of the rotated 3 km Albers square (351x316 px), neighbours overlap ~350-700 m, so the artefact is DUPLICATION (24 % of box polygons are "away" views, 15.6 % of area double-counted), not a cut at the lattice line. Proven from the files, not the code: raw `_segment.gpkg` vertex histogram in pixel space spikes 25x at px 2 and px 350 (own raster edge minus 2 px) and shows nothing at neighbour-edge columns; composites have no seam (gradient test at chance). A false lead on the way: a "boundaries coincide with neighbour raster edges" signal (27 % vs 3 % control at 200 m) turned out to be geometry of the test plus true-north-aligned Riverina fences -- the histogram killed it. The image test also showed that ~half of polygons flagged "cut" after the merge are real fences within 30 m of the raster edge, not cuts (3 of 4 eyeballed samples), which is why `raster_cut_m` is a flag, not a filter.
RESULTS (Riverina 60 km box, 2024): 10,304 -> 7,278 polygons; overlap 52,590 ha (15.6 %) -> 2,082 ha (0.73 %); 1,004 whole paddocks rebuilt from 2,078 views; 71 class disagreements reconciled + 19 confident ones kept, all flagged; 16 s. Same on 2023 (15.2 % -> 1.5 %) and the offset-grid 2024 pilot. Unions match the 2023 map at median IoU 0.86-0.93 vs 0.94 for interior untouched polygons (the year-to-year SAM ceiling) -- the unions are real paddocks. Residual after merge: 2,120 classified polygons still end at their raster edge; image test says 971 (46 %) are genuine (84 % of untouched, 92 % of rescued, 22 % of unions). Known-answer repair test (re-cut the merge's own 609 unions, repair from a donor that never saw them): offset-grid 2024 donor restores 65 % at median IoU 0.993 (from 0.818), 94 % >= 0.9, none <= 0.7, overshoot 0.11 ha; 2023 donor 44 % at 0.991. Applied for real: 178 repaired (+2,214 ha; 131 from the offset grid, 47 from 2023) -- low yield because residual cuts PERSIST across years (same neighbour tile fails on the same paddock), so a same-year offset-grid view is the better donor. The 4 "known split pairs" of sec 6: only pair 4 (and partly 2) were duplication; pair 3 is a real class disagreement between two barely-overlapping views; pair 1 (the original by-eye case) looks like two paddocks separated by a track along the lattice line, not an artefact.
BUG CAUGHT LATE: sqlite3 binds numpy.int64 as a BLOB with no error, so `UPDATE ... WHERE fid=?` silently matched nothing for the ~500 untouched polygons M2 clipped (in-memory overlap 2,123 ha, file 7,812 ha). Found by auditing the WRITTEN file independently; fixed with `_py()` coercion + `int(fid)` everywhere; whole evidence chain re-run on the fixed files. Lesson recorded in memory: audit the file, not the in-memory summary.
NOT DONE / NEXT: (1) national `boundary` pass per year once the re-predict lands (do not run on the retired-model 2023/2024 files); (2) a same-year offset-grid segmentation nationally is the only route to fixing persistent cuts and costs a second SAM pass (~GPU cost of `sam` per year) -- user decision; (3) the image-based region-growing alternative (extend a cut view into the neighbour composite without any donor) was scoped, not built; (4) tile-size benchmark (point 3b) still needs the GPU go-ahead.
2026-09-09 KSU BENCHMARK + TILE SIZE + OVERLAP (user: apply the shelterbelts GADI_MEMORY_QUEUE_COSTS.md lessons, benchmark to minimise KSU per national year, benchmark 9 km+ tiles for similar polygons at the same KSU, and test whether more overlap helps the edge cleanup). 25 PBS jobs, ~32 SU, on one 10x10 Riverina block (grid r932-941, c1182-1191). Report `output/BENCH_KSU.md` (generated by `src/paddocks/bench/bench_report.py`), figures `output/figures/bench_ksu/`, data `derived/benchksu/`.
RESULTS. (a) CPU stages: presegment RSS 0.5 GB, predict 1.1 GB (both far below their requests; "Memory Used" = page cache); walltime identical across queues; normalbw bills 8 GB as ONE core -> presegment 0.27 vs 0.44 SU, predict 0.54 vs 1.64 SU for identical tiles; outputs bit-identical (60/60 composites, 713/713 predictions incl. probabilities and yield). (b) SAM: production 3.0 s/tile, V100 54 % busy; 3 processes on one GPU gain 7 % (time-sliced); points_per_batch nothing; fp16 2.2 s with 99.6 % identical polygons; THE BIG ONE: samgeo reads a 3 km composite (351x316 px) into a 768 px zero-padded canvas and SAM prompts the padding, so 752 of 1,024 prompts decode nothing -- `--prompt-image-only` (272 prompts) gives 1.2 s/tile with polygons IDENTICAL to production (2,467/2,467 at IoU>=0.9), 0.8 s with fp16. (c) Projection (2024 stage costs x measured ratios): 6,617 -> 3,595 SU/yr (54 %) with normalbw + image-only prompts (no product change), 2,628 (40 %) adding fp16. (d) Tile size: 9/18 km tiles give the merged product's population (1.4-1.6 polygons/km2, 39-42 ha median vs merged 3 km 1.48, 39 ha) but only 58-59 % of production polygons at IoU>=0.5 (merge alone: 76 %; year-to-year ~75 % at 0.7) -- a different product, not the same paddocks with fewer seams; SAM per km2 only 10 % cheaper at 9 km (same 512-px windows per km2), 768-px windows halve it at further agreement loss; single-pass 9 km under-segments badly (0.49/km2). predict RSS 6.6 GB at 9 km, 24.8 GB at 18 km (holds the year cube). (e) Overlap: 4 km tiles on the 3 km lattice (1 km overlap) -> after the merge, raster-edge cut share 0.405 -> 0.162 and band density ratio 0.95, but SAM 10.9 s/tile (more image in the canvas => more prompts/masks) and 58 % agreement with production; to be re-measured with the prompt grid before adoption.
APPLIED IN CODE (this commit): presegment.pbs, predict_tile.pbs, boundary_national.pbs -> normalbw; run_national.sh passes SAM_EXTRA (default "--prompt-image-only", rebuilt per composite; fp16 opt-in) to sam/resam/repair-sam; samgeo_segment.py gained --sample-size/--bound/--no-batch/--points-per-batch/--fp16/--prompt-image-only. merge_tile_boundaries.Lattice now infers spacing from centres (overlap tiles). NOT re-run: nothing national. VERIFIED: the per-composite grid on 10 further tiles (job 178481584) reproduces production exactly (298/298 polygons at IoU>=0.9, 1.3 s/tile). RECOMMENDATION: do not re-segment past years for edge artefacts (merge + flags cover it; a new tile size changes ~40 % of boundaries and breaks the multi-year consensus); if a future product may differ from the series, 9 km default-window tiles are the sensible size, validated on the labelled sites first.
2026-09-09 TILE GEOMETRY DECISION (user: re-measure the 1 km overlap with the prompt fix, try 9 km + overlap and up to 2 km overlap, find out whether the ~40 % differing polygons are bad in production or in the new method, write it up with example images and gpkgs; fp16 + the other 3 changes approved for the national runs). 13 more jobs (~5 SU): composites for 5 km / 10 km / 11 km tiles, two GPU jobs (all arms with `--prompt-image-only --fp16`), six predict jobs (36 or 4 tiles each, production model + gates), then class-aware merges per arm on its own lattice. Report `output/TILE_GEOMETRY_DECISION.md` (generated by `bench/geometry_report.py` from `bench/geometry_decision.py`), figures `output/figures/tile_geometry/`, example gpkgs `output/examples/tile_geometry/` (production block merged vs 9 km block merged, plus the unmerged inputs).
DECISION: 9 km tiles, no extra overlap, default merge. Block interior after merge: raster-edge cut share 0.155 vs 0.569 (production), residual overlap 2.4 % vs 1.1 %, same classified area (17.1k vs 17.0k ha), 0 class conflicts; 75 % of production polygons matched at IoU>=0.5 (65 % at 0.7), class agreement 92 % on matches. Cost 2,176 SU/yr on the real 9 km grid (15,968 parents; `grid9_from_2024.py`) = 88 % of the optimised 3 km run (2,476) and 33 % of 2024 as run. Overlap on the 3 km lattice FAILS with real classes: 63-74 % away views, residual overlap 7.9-14.4 %, cut share 0.70-0.77, cost 153-231 % of optimised 3 km; the no-rescue rule (new `--cover-min 0` path, `no_twin_no_rescue`) fixes the overlap but drops 4-8 % of classified area. 9 km + overlap: nothing under default rules; 11 km on the 9 km lattice with no rescue gives the cleanest geometry (cut 0.048, 0 % overlap) at -12 % classified area and +39 % cost -- the alternative if seams matter more than coverage. VISUAL REVIEW (6 windows where the products disagree most, true colour + composite + both merged products): production is the fragmented one in 4 of 6 (lattice-aligned pieces the merge could not restore, incl. one paddock cut into quadrants), 1 draw, 1 class-only disagreement. Nothing looked broken by the 9 km tiling.
ALSO: `run_national.sh` gained an `M=` override so a second tiling of a year gets its own directory; `grid9_from_2024.py` writes the 9 km grid from the 2024 3 km grid. The user will start a new session to re-run 2024 at 9 km (commands in the report sec 6) and compare with the retired 3 km map, then continue with the other years.
2026-09-09 FUSED PREDICT QUESTION (user: would reusing the Sentinel-2 bands from segmentation for predict, instead of re-reading the datacube later, save compute? Sonnet said no benefit; wanted Fable's benchmarked opinion before the 2024 re-run). Two jobs, ~4 SU. `output/FUSED_PREDICT_BENCHMARK.md` (generated by `bench/fused_report.py` from `bench/read_split.py` + `bench/bench_fused.pbs`). ANSWER: (1) band reuse is not possible here (SAM on the GPU sits between the two reads; holding the cube needs 1.4 GB per 9 km tile = 22 TB nationally, 15 TB at 3 km) and its ceiling is small anyway: the two stages read different things (presegment green+NIR+fmask whole year in 6933; predict 10 bands+fmask DOY 90-350 in 3577), so read-once saves only the presegment read, 37 s of a 215 s per-9-km-tile CPU budget (17 %), ~200 SU/yr (8 %). Sonnet was right on that. (2) The reachable win is CO-SCHEDULING: a gpuvolta job bills 12 cores and SAM uses one; with 11 predict workers on the same node SAM slowed <=5 % and predict ran at normalbw speed (93 vs 91 s per 9 km tile). At 9 km SAM wall (10.6 s/tile incl. read/polygonise/write overhead) exceeds predict/11 (8.5 s), so predict rides free: 9 km year 2,498 -> 1,992 SU (-20 %). At 3 km it would be predict-bound and WORSE (5,490 vs 2,903). Recommendation: run 2024 at 9 km with separate stages now; build the SAM+predict orchestrator (workers consuming tiles as _filt.gpkg lands) as the next optimisation. ALSO CORRECTED: the geometry decision's SAM cost had used GPU segment seconds only; now priced by job wall per tile (read+polygonise+filter+write add 20-50 %): 3 km 1.23 s (0.80 GPU), 9 km 10.6 s (8.55). TILE_GEOMETRY_DECISION.md regenerated: p9 2,500 SU/yr vs 3 km 2,904 (86 %), 38 % of 2024 as run; decision unchanged.
2026-09-09 9 KM VALIDATION AT THE TEST SITES (before the 2024 re-run; `bench/site_tiles.py`, `eval_map_at_sites.py`, `bench/validation_report.py` -> `output/VALIDATION_9KM.md`). Both products scored at the 543 fixed test trials (533 on the lattice) with the temporal-holdout copy of the adopted model (`group3_map_sharma6_le2022.joblib`, trained on 2017-2022). WHAT RAN: 9 km composites in EPSG:3577 (4 normalbw jobs, 268 parents, 11.5 SU, all OK); the block arm in the final configuration (`sp_p9_3577`, 4 tiles, OK); SAM+predict co-scheduled for the 9 km site parents (2 gpuvolta jobs); 3 km site predictions (2 normalbw 1-core jobs). RESULTS SO FAR: 9 km 2024 complete (137 parents, 41 SU, 30 s wall/tile) -> macro F1 0.920 at 177 scored of 270 sites (Canola 0.905 / Cereal 0.955 / Legume 0.901), balanced acc 0.919, polygon median 49.5 ha vs trial paddock median 54.5 ha (median |log ratio| 0.013, 93 % within a factor of 2). 9 km 2023 PARTIAL: 64 of 131 parents predicted before the 2 h walltime (72.7 SU); macro F1 0.820 on the 75 sites scored so far. THREE FAILURES, ALL FIXED: (1) `val_sp9_2023` (node gadi-gpu-v100-0010): SAM ran at the normal 9 s/tile but predict's zonal step took 482 s/tile (5 s/polygon) vs 16 s/tile in the identical 2024 job on another node, 8.6 of 12 cores busy -> thread oversubscription (each numpy worker spawning a thread per node core). `segment_predict.py` now pins OMP/OPENBLAS/MKL/NUMEXPR to 1 thread per worker; `predict_tile.pbs` and `bench_predict.pbs` export the same. (2) Both 3 km jobs (141 / 148 scattered tiles) hit their 4 h walltime at ~175 s/tile with the GeoPackage unwritten (predict_tile.py writes at the end) -- 10 SU lost. (3) `eval_map_at_sites.py` pooled two years' predictions without a year filter, matching 92 of 360 sites to the other year's polygon (pooled macro F1 0.749 vs 0.82 / 0.92 per year); it now matches a site only to polygons whose stub carries its year. Also the label area column was empty (labels_combined has area_ha for 669 of 5148 rows; the test set carries it as paddock_ha) -- `site_tiles.py` fixed and the site table regenerated. RESUBMITTED 19:20: three predict-only top-ups on normalbw (4 workers each, `sampredict.pbs` with the new `ORCH_EXTRA=--no-sam` passthrough, 5 h walltime, expected ~2 h): `val_top9_2023` (178542388, 67 parents), `val_top3_2023` (178542391), `val_top3_2024` (178542392). `waiter_final.sh` scores everything and writes the report when they land; `geometry_decision.py --only p9_3577` (now merges into the existing JSON) runs on the login node for the report's edge-artefact table. The user's 55 `pred_2022` jobs in the queue belong to the shelterbelts project, not this one.
2026-09-09 (later) VALIDATION RESULTS AND A CORRECTION. All three top-ups finished (val_top9_2023 67 parents in 2.6 h; val_top3_2023 141 tiles in 1.9 h; val_top3_2024 148 tiles in 2.2 h; ~20 SU together), `output/VALIDATION_9KM.md` written. POOLED, 533 sites, temporal-holdout model on both: 9 km final macro F1 0.898 (Canola 0.893 / Cereal 0.952 / Legume 0.848, balanced acc 0.900, 349 scored) vs 3 km production 0.885 (0.890 / 0.948 / 0.817, 0.882, 367 scored). Per year: 2023 0.878 vs 0.869; 2024 0.920 vs 0.907. The 9 km product leaves more sites without any polygon (122 vs 94 of 533) and abstains on fewer (62 vs 72); coverage-adjusted recall Canola 0.48 vs 0.55, Cereal 0.65 vs 0.70, Legume 0.63 vs 0.55. Polygon area vs trial paddock area: median |log ratio| 0.020 vs 0.097, within a factor of 2 for 91 % vs 80 % (polygon median 49.8 vs 45.5 ha, label median 57.2 ha). Block arm in the final configuration (EPSG:3577, 350 m buffer): cut share 0.166 vs 0.569 production, residual overlap 0.91 %, 0 class conflicts, 71 % of production polygons matched at IoU>=0.5. CORRECTION to the entry above: the val_sp9_2023 slowdown was NOT thread oversubscription. With one BLAS thread per worker on normalbw the same 2023 9 km tiles still ran zonal at 208 s/tile (2024 9 km: 16 s; 3 km 2023 in the same regions: 17 s); read times normal throughout. It is a property of the 2023 9 km tiles' data. Thread pinning stays (free). Two 1-core cProfile jobs (prof_2023 178553912, prof_2024 178553913, one tile each, ~0.3 SU) are running to find it. COST: the report's per-tile SU from these jobs (0.30 SU/tile, 6,003 SU/yr) is an upper bound -- scattered parents read at 188 s/tile vs 93 s on the contiguous benchmark block, so the 2024 job was predict-bound; on the real grid the FUSED_PREDICT_BENCHMARK figures (~2,000 SU SAM+predict, ~2,500 SU/yr) apply only if the 2023 anomaly is resolved. DECISION INPUT: at the sites the 9 km product is at least as accurate as the 3 km one and its polygons match the trial paddocks better; the 2024 re-run at 9 km can go ahead, but resolve the 2023 zonal cost first or budget 2023-like years at 2-3x.
2026-09-09 USER DECISION (launch gate for the 2024 re-run at 9 km): start it only when all three hold -- (1) the predict zonal slowdown is understood and controlled, (2) a full national year at 9 km is confirmed to cost under 2.5 KSU, (3) queue headroom is free of the shelterbelts project's jobs. With all three satisfied the run is pre-approved; before that, do not launch. Diagnosis so far: profile shows the 2023 and 2024 tiles cost the same alone (zonal 104 vs 111 s on Broadwell), so it is not the year; three same-tile runs on three Broadwell nodes gave zonal 48 / 97 / 396 s at identical RSS (4.9 GB), with the 24 GB job slowest and 3x the CPU, so it is not the cgroup limit or threads either. The zonal phase is dominated by fresh large allocations (astype/where/stack over (t,y,x) arrays); numpy 1.26 hints MADV_HUGEPAGE on every large array and the nodes run THP enabled=madvise, defrag=madvise, i.e. synchronous compaction on exactly those faults -- the classic node-dependent stall. Six one-tile jobs (thp_on1-3 vs thp_off1-3, NUMPY_MADVISE_HUGEPAGE=0) with /usr/bin/time and /proc/vmstat compact_stall deltas are running to confirm.
2026-09-09 ZONAL SLOWDOWN SOLVED: THP COMPACTION STALLS ON numpy's MADV_HUGEPAGE. Six one-tile jobs (same 2023 9 km parent, 1 core normalbw, GDAL_CACHEMAX=512): with numpy's default huge-page hint zonal took 41 s (bdw-0001) and 89 s (bdw-0010), earlier runs 97-396 s; with NUMPY_MADVISE_HUGEPAGE=0 it took 25, 24 and 22 s on bdw-0010 and bdw-0013 (the node that had taken 396 s). /usr/bin/time: user time 47-48 s in every run; system time 32-80 s (hint on) vs 7-11 s (off); /proc/vmstat compact_stall rose by ~16k during the slow run. Nodes report THP enabled=madvise, defrag=madvise, uptime 36 days, numpy 1.26.3 madvise-hugepage default True. So: the same code stalls in the kernel for a node-dependent time on every large fresh allocation (astype/where/stack over the (t,y,x) cube), which is why the 2023 co-scheduled job (node 0010, 482 s/tile) and the 2024 one (node 0115, 16 s/tile) differed 30x with identical inputs. Not the year, not threads, not the cgroup limit. FIX APPLIED: `export NUMPY_MADVISE_HUGEPAGE=0` in every .pbs under src/paddocks and bench/ (inserted after `export PROJ_NETWORK=OFF`), and in segment_predict.py's WORKER_ENV. Presegment (same kind of full-cube numpy work; its 60-213 s/tile spread is probably the same effect) gets it too. Intrinsic zonal cost with the fix: ~22-25 s per 9 km tile on Broadwell, 16 s on the gpuvolta Skylake nodes. VALIDATION_9KM.md regenerated with the cause. GATE STATUS: (1) satisfied; (2) pending a like-for-like re-measurement of the co-scheduled job on contiguous 9 km tiles with the fix; (3) queue at 190 shelterbelts jobs (131 pred_2022 + 59 pi_21_lat*) at 23:00 -- not satisfied yet.
2026-09-09 23:45 GATE-2 PILOT (`output/PILOT_9KM_COST.md`, `bench/pilot_cost.py`): 48 contiguous parents of the real 9 km 2024 grid (Riverina; grid at benchksu/pilot9/aois_9km_2024.csv, 15,968 parents), presegment in EPSG:3577 on normalbw (2 jobs, 1.38 SU, median 77 s/tile -> 425 SU/yr) then one co-scheduled SAM+predict gpuvolta job with the THP fix (178560809: 48 tiles in 14.6 min, 8.77 SU, 0 failures). Measured: SAM wall 9.3 s/tile with the GPU busy 93 % of the SAM stage (segment mean 8.5 s) -- SAM is GPU-bound, a second SAM process would gain <10 %; predict mean 114 s/tile (read 87, zonal 26 -- the THP fix holds on the GPU node too) / 11 workers = 10.3 s, so SAM and predict are balanced at 10.3 s/tile. PILOT-LITERAL projection 3,392 SU/yr is a small-job artefact: startup (71 s) + the last batch's tail (~230 s) are 34 % of a 48-tile job's wall. STEADY STATE (wall = startup + n x 10.3 s + tail): 400-tile jobs 2,242 SU/yr, 1,000-tile jobs 2,171 SU/yr, 2,000-tile 2,147 -- i.e. ~13 % under the 2,500 SU gate before any repair pass (2024's 3 km run spent ~5 % on one). RECOMMENDED SIZING for the run: NCHUNK=60 (266 tiles/chunk) x PER_SAM=4 -> ~1,060-tile SAM+predict jobs, ~3 h each inside SP_WALLTIME=06:00:00 (the default PER_SAM=8 would be ~6.1 h and hit the walltime). GATE STATUS 23:45: (1) satisfied; (2) expected 2,170-2,240 SU by the steady-state model, but the only direct measurement is the 48-tile pilot at 3,392 -- whether a modelled figure counts as "confirmed" is the user's call; (3) NOT met: 131 shelterbelts jobs (86 pred_2022, 44 pi_21_lat) still running. The run has NOT been started; nothing exists under national2024_9km/.
2026-09-10 USER DECISION: gate 2 accepted on the steady-state model (option 1). The user has paused submitting shelterbelts jobs; the 2024 9 km national run is to be launched once fewer than 10 shelterbelts jobs remain in the queue. Launch is pre-approved from that point; nothing else needs asking. Run directory $D/national2024_9km (M= override), grid = benchksu/pilot9/aois_9km_2024.csv (half_m 4850), NCHUNK 60 / PER_SAM 4 sizing per PILOT_9KM_COST.md.
2026-09-10 00:20 2024 9 KM RUN ARMED. run_national.sh: presegment takes PS_MEM/PS_WALLTIME; `sampredict` rewritten to be re-runnable (skips groups whose tiles are all in done_stubs.txt or with a live sp_s<k> job, MAXQ guard, logs $M/sam/spjobs.txt) with SP_NODEP=1 = submit a group only when all its composites exist and with no PBS dependency (the vanishing-dependent-job hazard); `status` now counts sp_s jobs. Run dir $D/national2024_9km: aois.csv = the 15,968-parent 9 km grid (half_m 4850), 60 chunks of ~266 (block-sorted). Dry pass of sampredict submitted nothing (15 groups waiting). Presegment peak RSS at 9 km 3.1-3.5 GB -> PS_MEM=8GB. launcher_2024_9km.sh (nohup) polls the shelterbelts count every 10 min and launches at <10; milestones to validation/waiter.log with prefix run2024_9km:. Cost log: `./run_national.sh cost` (sums every log in the logs dir).
2026-09-10 01:30 USER DECISION REVISED -> OPTION 2. One production-size slice now, while the shelterbelts jobs are still running: presegment chunk p000 (267 parents, ps_p000 = 178568915, normalbw 8 GB, 10 h) and, when its composites land, one SAM+predict job on it (sp_s0 via `PER_SAM=1 SP_NODEP=1 run_national.sh sampredict`; later PER_SAM=4 regroups sp0 as p000-p003 and resumes from done_stubs). The launcher (launcher_2024_9km_v2.sh, the v1 one was stopped before it fired) measures both jobs' SU, writes $M/pilot_result.txt and projects the year as (SU_ps + SU_sp) / tiles x 15,968 + 50. DECISION RULE (user): when fewer than 10 shelterbelts jobs remain, if the projection is > 4,000 SU halt and re-evaluate compute-reduction options; otherwise submit the full 2024 run without asking. Expected from PILOT_9KM_COST.md: ps ~7 SU + sp ~31 SU for 267 tiles -> ~2,300 SU/yr.
2026-09-10 08:55 2024 NATIONAL RUN AT 9 KM LAUNCHED. Pilot slice (chunk p000, 267 parents): presegment 7.85 SU (267/267 built, 0 failed), SAM+predict 33.32 SU (267/267 predicted, 0 failed batches, 3,315 s = 12.4 s wall/tile incl. startup+tail) -> 0.1542 SU/tile -> projected year 2,512 SU (gate: <= 4,000; user's original target 2,500). Gates at launch: (1) THP fix in; (2) projection 2,512; (3) 0 shelterbelts jobs in queue. Launcher submitted 59 presegment jobs (all live, none vanished -- checked every id against qstat/logs), 1 chunk already done; 15 SAM+predict groups (PER_SAM=4, ~1,060 parents each) will be submitted as their composites complete, then merge -> boundary -> summary. Expected: presegment ~6 h per job; whole year ~1-1.5 days wall. Progress lines every 2 h in validation/waiter.log and $M/launcher.log.
2026-09-10 MANUSCRIPT BROUGHT UP TO DATE (user: reassemble MANUSCRIPT_DRAFT.md, reconcile sections_revised, apply the shelterbelts2 WRITING_STYLE.md before their own edits). sections/ was 2 days ahead of the assembled draft (09-07/08 citation+wording edits to 01/02/03/06/07/09 never reassembled); sections_revised/ was a dead 09-04 snapshot of sections/ (review loop applied fixes in place, per REVISION_LOG.md) -> deleted. New output/manuscript/assemble.py rebuilds the draft from sections/ (front matter stripped, filename order; Declarations now before References). House-style pass on all 12 sections via exact-string replacement scripts (116 edits, every target matched once): 0 em-dashes (was 71), no Oxford commas, Australian spelling, passive Methods, no bold in body lists, meta-commentary and reviewer-bait cut, long sentences split; numbers and claims unchanged. Model-adoption facts folded in (independent review ADOPT 09-07, adopted in code 09-08 commit 9b21c08, 2024 9 km re-run launched 09-10): Lim 10 + Table 8 + FW 1 and 6 + Disc 6.5 + Lim 6 + Conclusion; title block now says every result is still the three-index model. 3 cross-ref errors fixed (3.1 Lim 1->4; 4.6 Sec 5.2->5.3; 4.1 names Lim 3); Table 2 last row had the next paragraph glued on, split. NOT DONE: Results/Methods rewrite for the adopted model and 9 km geometry (waits on the re-run); paper-covert (both submission packages are 09-04 and stale). Draft ~10.5k words, down from 11.9k.
2026-09-10 (later) MANUSCRIPT SECOND PASS (user: XXX for superseded results, lowercase class names, no AgriWebb, commit before their edits). Baseline committed first as ec20cf4 (house style + reassembly). Then: every map-derived number from the first national map -> XXX (103 placeholders; Figs 2/3/4/5/7a/8 marked regenerate); 5.1 + Table 2 now report the adopted nine-index classifier from GROUP3_MODEL_shipped_plus_sharma6.md (0.890/0.892, per-class P/R/F1, confusion counts), the 3->9 index gain is an ablation in 5.1 and 4.3 describes both stages, so Lim 10 / Table 8 / FW 6 removed (FW 7 -> 6, Conclusion two threads); design-stage ablations kept with their feature set / run named. Lowercase canola/cereal/legume/wheat/barley/oat except sentence starts and table headers (47 changes). AgriWebb removed entirely: 4.4 + 6.4 grazing-negative story and 3.4 filter retention figures (77.9/65/22 were AgriWebb-measured, PRESENCE_ONLY_LABELS.md s2) -> XXX against NVT paddocks. Draft 10.2k words.
2026-09-10 E8 2024 NATIONAL RUN AT 9 KM COMPLETE. SAM+predict finished 15/15 groups clean (2,078.5 SU). The launcher's merge (178687633) merged 0 chunks: `run_national.sh merge` linked the outputs as `sp<k>_p_<batch>.gpkg`, which the `p*.gpkg` glob in merge_national.pbs and abs_national.pbs never matched, and the launcher then waited forever for a file that would not appear (no HALT). Fixed (links `p_sp<k>_<batch>.gpkg`, merge stops on zero input), launcher retired by hand, re-merge 178692909: 1,238,529 polygons = sum of the 3,993 batch files. Boundary 178694520 (2.94 SU): 1,158,824 polygons. Map total ~2,538 SU against the 2,512 projection. Products, comparisons and costs: output/NATIONAL_2024_9KM_RESULTS.md. Headline: ABS canola r 0.77 (n = 151 SA2s), MAE 4.1 points, over-call 1.18x (1.31x before de-duplication, 1.47x on the retired 3 km map); canola now 2.4 points below its diluted share (was 0.3); WorldCereal presence agreement no longer symmetric (80.9 / 66.8%); NLUM legume enrichment 0.92 (was 1.62). "Classified" counts the 105,627 no_crop_shape polygons, which keep their class under --shape-gate-skip-classes; _good.gpkg is the strict mask. Dead 3 km stages and 26 scripts removed (output/DEAD_CODE_REMOVAL.md); run_national.sh had two predict) labels, so the real 3 km predict was unreachable. Manuscript (output/manuscript/sections) filled from the re-run with [CHECK] markers on changed claims, plus three all-years TEMPLATE sections (4.8, 5.8, 6.8). GEE: 9 km map uploaded to .../paddock_species_national_2024_9km_crops. The session that started the launcher and the launcher itself were killed together when the VSCode SSH session ended (NCI logind KillUserProcesses=yes).
